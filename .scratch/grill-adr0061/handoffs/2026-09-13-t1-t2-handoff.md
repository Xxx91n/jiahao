# Handoff - ADR-0061 implementation round: T-1/T-2/T-4/T-5 landed, T-3 remains (2026-09-13)

Repo D:/Aworker/jiahao. Branch `adr0061-impl` (stacked above `adr0061-doc`).
Commits: `ylv` T-1, `wop` T-2, `toq` T-2 closure, `mrs` defer-0037, `mvw` T-4, `kxk` T-5.
Tree clean. Task book: `.scratch/grill-adr0061/handoffs/next-round.md`.
Full evidence: `.scratch/grill-adr0061/reports/2026-09-13-report.md`.

## State (measured)
- `npx jest --runInBand` -> 53 suites / 708 tests, all passing.
- `node scripts/run-gates.js` -> exit 0 (4 unverifiable = local `ci-mode` absence).
- `npm pack --dry-run --json` -> 210,490 B / 93 files; ADR-0039 D3 cap 230,000 B.
- `node scripts/instrument.js --check` -> OK; chain valid through seq 8.

## Remaining: T-3 (D-002) identity surface narrowing
The only unfinished task. Read the report section "NOT done: T-3" for the exact
mechanism, the re-pin dance, the hash derivations, and the escape hatch. It is not
half-started: nothing about the identity was touched.

## Open items
- T-3 needs an independent human signoff for the identity re-pin (as T-2 D-E did).
- Findings 1-3 in the report (authorization not persisted; record() log seq; bias-probe
  artifact under gitignored .scratch/).

## Suggested skills for the next session
- `implement` (drive T-3 with tdd at the pre-agreed seams), `but` (writes), `handoff`.
