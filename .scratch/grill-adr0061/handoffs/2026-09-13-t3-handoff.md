# Handoff - ADR-0061 implementation round: T-3 (D-002) landed - round complete (2026-09-13)

Repo D:/Aworker/jiahao. Branch `adr0061-impl` (stacked above `adr0061-doc`).
Commit `lty` (git `01c1138`) = T-3 D-002 identity surface narrowing.
Prior commits this round: `ylv` T-1, `wop` T-2, `toq` T-2 closure, `mrs` defer-0037, `mvw` T-4, `kxk` T-5.
Task book: `.scratch/grill-adr0061/handoffs/next-round.md`.
Full evidence: `.scratch/grill-adr0061/reports/2026-09-13-report.md` (T-3 sections).
No push, no PR.

## State (measured, post-commit)
- `npx jest --runInBand` -> 54 suites / 720 tests, all passing.
- `node scripts/run-test-gate.js --expected-suites 54` -> exit 0.
- `node scripts/run-gates.js` -> exit 0 (22 entries, 4 unverifiable = local ci-mode absence).
- `npm pack --dry-run --json` -> 211,617 B / 93 files < 230,000 B cap.
- `node scripts/instrument.js --check` -> OK; chain valid through seq 10
  (certification conditional, expires 2026-12-11, CAPA-0060-judge-flip-rate).
- `node scripts/check-deferred.js` -> OK, 32 entries; `build-adr-index --check` -> in sync (63).
- Working tree: clean except one untracked file
  `bench/polygraph/results/reverify-20260913.json` (see Open items).

## What T-3 delivered
- Judge identity now hashes only the verifier-rules surface of `src/SKILL.md`
  (`## Verifier Profile` -> EOF); product edits no longer re-pin, judge-rule edits do.
  A missing heading throws (fail-closed, no whole-file fallback).
- Pin re-pinned: rules_digest f84a6c95..ee95c -> f6c6c843..c6154; triple_hash
  fb30005f..a71c -> c68d49c6..58b9; state chain seq 9 quarantine + seq 10 conditional sign-off.
- ADR-0063 + `defer-0038` (review_at 2026-12-11) + `test/adr-0063-wiring.test.js` (12 tests:
  both polarities + fail-closed) + ci/README counts 54/720.

## Deliberate deviation (read before reviewing)
The task book prescribed a plain `--signoff`. The reverify ledger tail is
`indeterminate` (n=26 < min_n=100), so the ADR-0060 D-E guard refuses a plain certify;
the re-pin used `--conditional-signoff` (reviewer Euiop1, second_reviewer Xxx91n != author,
expires 2026-12-11, CAPA-0060-judge-flip-rate), which is what the ADR-0060 round itself used.
Result state is the same conditional certification, now carrying the narrowed identity.

## Open items
1. `bench/polygraph/results/reverify-20260913.json` was produced by the instructed
   `npm run reverify` but NOT committed (round constraint: never touch that dir).
   The human reviewer decides whether to archive it; it is regenerable byte-for-byte.
2. A full certify stays unreachable until the corpus reaches min_n=100 (defer-0032).
3. The `## Verifier Profile` heading is now load-bearing (fail-closed rename).
4. Earlier findings still open: authorization not persisted on `criteria_change` /
   `record_signoff`; `record()` logs the first record seq; the bias-probe artifact
   lives under gitignored `.scratch/`.

## Suggested skills for the next session
- `handoff`, `but` (writes), `implement` (T-6 product round: register first, code second).
