# ADR-0061 Implementation Round - Repair Sub-Agent Report (2026-09-13)

Branch `adr0061-impl` (stacked above `adr0061-doc`). Commits: `ylv` T-1, `wop` T-2,
`toq` T-2 D-E closure, `mrs` defer-0037 tide, `mvw` T-4, `kxk` T-5 (+ amended count fix).
Working tree clean; no push, no PR (WORKFLOW 4.2).

## Completed

**T-1 D-006** (`ylv`) - `bench-corpus` probe now requires an existing AND non-empty
dir (absent/empty -> exit 2 UNVERIFIABLE); ADR-0040 D1 amended same-commit. gate:all
now asserts the ADR-0039 D3 cap via single-sourced `packCapBytes()`, after the
packaged-CLI smoke.

**T-2 D-001** (`wop`, `toq`) - new ADR-0062: policy before value; protocol pinned
(`npm pack --dry-run --json`, `size`, npm 10.9.7); cap derived from the on-file trend
by `ceil_to_10_000(M_latest x 1.10)` = 230,000 B; explicit non-retro-application
declaration vs the withdrawn 5567bd8; ADR-0039 D3 one-shot M anchor -> periodic trend
anchor. D-E countersigned by `Xxx91n` via the ADR-0047 criteria-change path:
`src/instrument-state.json` seq 6 (`criteria_change`, attestation approve,
`restatement_of: 5567bd8`, event_hash 17de5b7c...0355). `defer-0037` registers the
review_at (2026-12-13) in the tide (`mrs`).

**T-4 D-003** (`mvw`) - `src/instrument-state.json` seq 7 `record_only_change`
(maker Euiop1): model_checkpoint/model_identity sub-declarations tagged
out-of-service (declaration-scoped), re-verification path P-A1, deadline 2026-12-11
(defer-0035); seq-3 disposition (conditional interval stands, ADR-0060 D-B
indeterminate); look-back over 2026-09-12..13 (no impact). seq 8 `record_signoff`
countersigned by `Xxx91n` (!= author), attestation approve. Append-only, no rewrite;
no new state machine kinds; hash chain verified.

**T-5 D-005** (`kxk`) - new git-tracked `docs/governance/` OUTSIDE the tarball
whitelist: `decision-ledger-adr0059.md`, `decision-ledger-adr0061.md`,
`audit-report-adr0058.md`, `ERRATA.md`. Machine-generated `anchors.json` +
regen-and-diff check (`scripts/build-governance-anchors.js --check`; write and
--check share one path). ADR-0059 ledger pointer line amended (C-1). `defer-0024`
minimal unlock (C-2) recorded in its check-in; the cross-domain witness stays
deferred. New wiring test + a tarball-absence assertion.

## Verification loop (re-runnable)

| command | result |
| --- | --- |
| `npx jest --runInBand` | **53 suites / 708 tests, all passing** |
| `node scripts/run-gates.js` | **exit 0** (22 entries, 4 unverifiable = local ci-mode absence) |
| `npm pack --dry-run --json` | **210,490 B / 93 files < 230,000 B cap**; `docs/governance` NOT packed |
| `node scripts/instrument.js --check` | OK; chain valid through seq 8 |
| `node scripts/check-deferred.js` | OK - 31 entries |
| `git diff --check` | clean |

Acceptance: build green; pack green; launch-and-smoke green (gate runs the packaged
CLI); every suite closed by tests.

## T-3 (D-002) identity surface narrowing - COMPLETED

Status update: DONE this round in commit @@BT@@01c1138@@BT@@ (but id @@BT@@lty@@BT@@) on
@@BT@@adr0061-impl@@BT@@; full evidence is in the T-3 section at the end of this
report. The notes below are the pre-implementation mechanism record, retained
verbatim.

### (pre-implementation mechanism notes, retained)

Deliberately not started, not half-done: the narrowing changes the resolved
`rules_digest`, so the pin and the identity state must move together. Mechanism
reverse-engineered for the next agent:
1. Partition `src/SKILL.md` at `## Verifier Profile` (line 57; the `---` at line 55 is
   the product/judge boundary). Judge surface = that heading to EOF; product surface =
   frontmatter + H1 + Generator Profile.
2. In `resolveInstrumentIdentity` hash `judgeSurfaceText(rulesText)` instead of the whole
   file; fail closed if the heading is absent.
3. The digest changes -> the pin (`src/instrument-identity.json` `rules_digest`) must be
   updated AND the identity re-pinned through quarantine -> signoff
   (`--quarantine`; `npm run reverify`; `npm run judge:bias`; then
   `--signoff --reviewer <id> --attestation certify --reverify-ledger-hash <h>
   --bias-probe-hash <h>`). A `criteria_change` cannot carry it (its branch requires
   the digest to equal the authoritative one). `--rollback` is the escape hatch.
4. Hash derivations (verified): `reverify-ledger-hash` = the ledger tail `event_hash`;
   `bias-probe-hash` = sha256 of the bias-probe artifact (the value recorded at seq 3
   equals sha256 of `.scratch/grill-adr0059/evidence/bias-probe.txt`).
5. Add fixtures proving both polarities (product edit -> no re-pin; judge edit ->
   re-pin), an ADR, second_reviewer and review_at.

## Findings for the next round

1. `criteria_change` and `record_signoff` consume `--authorization` (the CLI guard
   demands it) but do NOT persist it in the event; only `signoff` does. The verbatim
   human authorisation is therefore dropped from the hash-chained record.
2. `record()` logs `seq=` by finding the FIRST `record_only_change` in history, not the
   one it just wrote (cosmetic, but misleading).
3. The bias-probe artifact lives under gitignored `.scratch/`, so a hash that anchors a
   human signoff points at untracked storage - a tension with ADR-0061 D-E.


## T-3 (D-002) identity surface narrowing - COMPLETED (evidence)

Commit: `01c1138` (git) / `lty` (but) on `adr0061-impl`. Tree green; no push, no PR.

### What changed

- `src/instrument-identity.js`: new `judgeSurfaceText(rulesText)` slices the rules text
  from the `## Verifier Profile` heading (line 57) to EOF; a missing heading
  throws (fail-closed, no whole-file fallback). `resolveInstrumentIdentity` now
  hashes that surface instead of all of `src/SKILL.md`. `JUDGE_SURFACE_HEADING` and
  `judgeSurfaceText` are exported.
- Digest moved: `rules_digest` f84a6c95..ee95c (whole file) -> f6c6c843..c6154
  (judge surface); `triple_hash` fb30005f..a71c -> c68d49c6..58b9.
- `src/instrument-identity.json` `rules_digest` re-pinned to the new value.
- New `docs/adr/0063-surface-narrowed-judge-identity-anchor-semantic-digest-rejected.md`
  (D-A narrowing, D-B fail-closed boundary, D-C semantic-digest ban + JudgeSense,
  D-D second_reviewer/review_at/re-pin record). README ADR index rebuilt (63 entries).
- `docs/deferred-registry.json` gains `defer-0038` (review_at 2026-12-11); the id is
  anchored in ADR-0063 text. `test/adr-0033-wiring.test.js` id/status arrays 31 -> 32.
- New `test/adr-0063-wiring.test.js` (12 tests): product-surface edits (frontmatter /
  H1 / Generator Profile) leave the digest unchanged; judge-surface edits (one word,
  EOF whitespace, iron-law wording) change it; a missing/renamed heading throws.
- `.github/workflows/ci.yml` `--expected-suites 53` -> `54`; README counts
  708/53 -> 720/54 (same commit, ADR-0057 D-C).

### Re-pin evidence (P-A1)

| step | command | result |
| --- | --- | --- |
| quarantine | `node scripts/instrument.js --quarantine` | exit 0; state quarantined; seq 9, identity c68d49c6..58b9 |
| reverify | `npm run reverify` | exit 1 (EXPECTED); ledger seq 5 `60127eb658d4bfc1e2fab0408507d804c8d52fde65258271cfac22a542263e01`, conformity `indeterminate` (n=26 < min_n=100), identity_triple carries f6c6c843..c6154 |
| bias | `npm run judge:bias` | exit 0; 8 pairs, 5/8 hits; recaptured output sha256 == 99a20aae..9904 (byte-identical to `.scratch/grill-adr0059/evidence/bias-probe.txt`) |
| signoff (plain) | `--signoff ...` | exit 1 REFUSED: "requires a pass conformity in the reverify ledger tail (use --conditional-signoff for indeterminate|conditional)" - no mutation |
| signoff (conditional) | `--conditional-signoff --reviewer Euiop1 --second-reviewer Xxx91n --attestation certify --authorization <verbatim> --reverify-ledger-hash 60127eb6..3e01 --bias-probe-hash 99a20aae..9904 --expires-at 2026-12-11 --capa-ref CAPA-0060-judge-flip-rate` | exit 0; seq 10 conditional_signoff, event_hash 09b9413e34658d55a49b9ddfb6a86790fafca5e6e21555a7e09508d7a1315d3d; verbatim authorisation anchored |
| pin | `src/instrument-identity.json` rules_digest -> f6c6c843..c6154 | `node scripts/instrument.js --check` exit 0 "identity pinned and authoritative (certification: conditional)" |

Deviation from the task book (reported, not smoothed): the task book prescribed a
plain `--signoff`. The reverify tail is `indeterminate`, so the ADR-0060 D-E guard
refuses a plain certify; the sanctioned path is `--conditional-signoff`, which is
what the ADR-0060 round itself used. The resulting state is the same conditional
certification as before the change (expires 2026-12-11, CAPA-0060-judge-flip-rate),
now carrying the narrowed identity. No assertion was weakened.

### Verification loop (final, post-commit)

| command | result |
| --- | --- |
| `npx jest --runInBand` | **54 suites / 720 tests, all passing** |
| `node scripts/run-test-gate.js --expected-suites 54` | exit 0; "54 suites, 720 tests, 0 skipped" |
| `node scripts/run-gates.js` | **exit 0** (22 entries, 4 unverifiable = local ci-mode absence) |
| `node scripts/instrument.js --check` | OK; chain valid through seq 10 |
| `node scripts/check-deferred.js` | OK - 32 entries |
| `node scripts/build-adr-index.js --check` | in sync (63 entries) |
| `npm pack --dry-run --json` | **211,617 B / 93 files < 230,000 B cap**; pack-smoke agrees |
| `git diff --check` | clean |

### Honest notes

1. `npm run reverify` exits 1 by design (conclusion indeterminate, n=26 < min_n=100).
   This is not a failure of the run; it is the ADR-0060 D-B honest statement.
2. `npm run reverify` wrote `bench/polygraph/results/reverify-20260913.json`. Per the
   round hard constraint ("never touch bench/polygraph/results/*") it was NOT
   committed; it is left untracked for the human reviewer (the reverify runbook
   "human commits the artifact" step). It is regenerable byte-for-byte.
3. The bias-probe artifact lives under gitignored `.scratch/` - the D-E tension the
   previous report already flagged. The re-pin reused it only after re-running
   `npm run judge:bias` and confirming the recapture is byte-identical.

### Findings for the next round (T-3 addendum)

1. `--signoff` (full certify) is unreachable while the corpus sample stays below
   `min_n=100`. Every identity re-pin therefore lands as a conditional
   certification and re-arms the same CAPA. Reaching a full certify needs corpus
   growth (defer-0032), not a process change.
2. The judge-surface boundary is now load-bearing: the `## Verifier Profile` heading
   is a fail-closed rename. Any future SKILL.md restructuring must keep it.

