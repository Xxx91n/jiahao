# grill-t27 round report - CI-red triage fix round

Date: 2026-09-25. Lane: `grill-t27` (GitButler). Base: `0ca482f7` (origin/main
tip at T-0; red run 36030223375). Facts canon: `round-facts.json` (renders
deterministically; report_commit stays null).

## 1. What was wrong and what changed

Six red legs on the last `origin/main` run, zero logic bugs:

| leg | root cause | fix |
| --- | --- | --- |
| corpus-leak / corpus-freshness / corpus-classes / mr-probes | `JIAHAO_BENCH_CORPUS_B64` tarball extracts to a partial/missing corpus; restore step only warned | restore step is now **validate-or-absent**: extract to sibling temp dir -> validate required-file set vs `bench/polygraph/thresholds.json` `private_corpus` manifest (id + sha256, consumed never hardcoded) -> mismatch deletes + `::error` + leaves corpus absent (registered exit-2 UNVERIFIABLE degrade downstream) -> match renames atomically into `$RUNNER_TEMP/bench-corpus` |
| sentinel-ownership D2b | unlink+recreate may reuse the inode on Linux | pre-create implant + `fs.renameSync(implant, f1)` - deterministic same-path inode change (POSIX rename(2) / MoveFileEx REPLACE_EXISTING) |
| adr-0079-wiring D5 | `npm-cli.js` assumed at `<bindir>/node_modules/npm` (Windows layout) | probe both layouts + `../lib/node_modules/npm` + PATH (`where.exe`/`which`) + realpath; invoke via `process.execPath` |

Files: `scripts/restore-bench-corpus.js` (new, zero-dep, ADR-0029 D4
core+CLI split), `.github/workflows/ci.yml` (step rewrite +
`--expected-suites 81->82`), `test/corpus-restore.test.js` (new, 7 rehearsal
legs), `test/sentinel-ownership.test.js`, `test/adr-0079-wiring.test.js`,
same-commit pin syncs (adr-0080/0081/0082 `expected-suites`, adr-0033 seed
inventory + defer-0072, adr-0074 `published_tip` -> 0ca482f7, README
1360/81 -> 1367/82 + zh-CN baseline -> f88b88be). `src/` untouched
(zero_product_diff true; assertions and probe contract preserved).

## 2. Evidence (every capture re-runnable via `node .scratch/grill-t27/capture-battery.cjs`)

Terminal wave, all captured-at-head `a1776dde`:

| claim | capture | verdict |
| --- | --- | --- |
| jest all suites | `node scripts/run-test-gate.js --expected-suites 82` | **82/82 suites, 1367/1367 tests, 0 skipped** (EXIT 0) |
| gates | `node scripts/run-gates.js` | EXIT 0, 36 entries; 4 UNVERIFIABLE (`ci-wiring`,`bench-gate`,`probes`,`mr-probes` - ci-mode absent locally, the registered degrade; corpus gates corpus-leak/freshness/classes/probe-corpus all PASS against private corpus) |
| governance inventory | `check-governance-inventory.js --coverage-base fc390d5e` | EXIT 0 (36 entries; two pre-existing advisory warnings only) |
| deferred registry | `node scripts/check-deferred.js` | EXIT 0, 66 entries (54 live, 12 closed/actioned) |
| README adr index | `build-adr-index.js --check` | EXIT 0 |
| rewrite map | `build-rewrite-map.js --check` + `--published-only` | EXIT 0 both (2836 citations, tip 0ca482f7) |
| anchors | `build-governance-anchors.js --check` | EXIT 0 (18 artifacts) |
| pack | `node scripts/check-pack-smoke.js` | EXIT 0, 353706 B tarball / 115 files / budget <380000 (ADR-0039 D3) |
| corpus restore rehearsal | real `private/bench-corpus` -> tgz -> `scripts/restore-bench-corpus.js` | EXIT 0 - validated 4 manifest entries, atomic placement incl. `mr-probes.jsonl` |
| liveness | npm pack -> tar extract -> `install.js --help` -> `install.js init -y --dry-run` -> `jiahao-mcp` JSON-RPC `initialize` | EXIT 0 - process starts and answers a JSON-RPC result object |
| freshness | `evaluateRound` over registered rounds | all rounds clean; t27 pre-seal evaluated 0 claims |
| clean tree | `git status --porcelain` | recorded verbatim (historical uncommitted audit pool intact - untouched by the round commits) |

Rehearsal matrix (jest `test/corpus-restore.test.js`, 7/7 PASS): complete
tarball -> rename-placed; missing `mr-probes.jsonl` -> absent + `::error` +
probe reads absent; stale sha256 -> absent; layout drift -> absent; extra
files tolerated; corrupt gzip -> degrade not crash.

## 3. T-3 secret-refresh package (owner action - not executed)

`D:\Aworker\jiahao\.scratch\grill-t27\secret-refresh-package.md` - tarball
7732 B / sha256 `7169fc72...`, base64 payload 10312 chars at
`.scratch/.tmp-t27-secret-refresh/JIAHAO_BENCH_CORPUS_B64.txt`, owner command
`gh secret set JIAHAO_BENCH_CORPUS_B64 < <file>`, post-set verification steps.
Secret write, pushes, merges, tag pushes stay owner-side.

## 4. T-4 bookkeeping

- `trend-inventory` `kind:fix` row with verbatim anchor quote (ADR-0040/0061
  D-F), cumulative R2 `governance_tooling_diff` (coverage base fc390d5e),
  `deferred_entry: defer-0072`.
- `defer-0072` registered (successor tracker; dual-gate trigger: 10
  consecutive UNVERIFIABLE runs or 2026-10-15 -> forced adjudication;
  pre-registered hook mints an ADR-0061-style revision ADR on first real
  firing; owner Xxx91n). Anchored in CONTEXT.md UNVERIFIABLE entry;
  adr-0033-wiring seed inventory extended to 66.
- ERRATA E-8 (cx/evd/rr naming) + E-9 (wave-1 evidence index) appended with
  reopen-triggers; t26 pinned artifacts untouched.
- defer-0070 remains `pending-evaluation` - closure is event-driven on the
  first `conclusion=success` `origin/main` run; the seven-field closure row
  is registered-ready (run_id / conclusion / verifiable_composition /
  degradation_semantics / degradation_cause / successor_defer_id=defer-0072
  / closure_rule_verbatim). No success run has landed; no closure claimed.

## 5. Convergence measurement (D-006; definitions+verdict in ledger D-009)

| metric | value |
| --- | --- |
| landed commits (grill-t27 lane) | 7 substantive pre-claim + claim + SEAL + post-seal regen |
| capture events, t25-legacy grouping | 4 (waves at 9c733e13 / 74654ce8 / e7285086 / a1776dde) |
| capture events, anchored | 1 (terminal wave) |
| anchored shadow captures | 0 |
| claim commits / freshness outcomes | 1 / first-pass 1, recapture 0, stale-retained 0 |

**Validity limits / threats:** n=1 single round on one repo. The two grouping
definitions measure different objects (diagnostic volume vs committed-claim
freshness); the delta (4 vs 1) is red-driven fix iteration, not wasted
evidence - waves 1-3 each caught a real red leg (anchors ordering,
published_tip drift, README count declaration) that drove the next commit.
This is an analytic description of this round's cost, not a steady-state or
causal claim about the seal machinery. The claim-commit captures satisfy
the floor exactly (at-head == last anchoring commit) - boundary case, not
margin-tested.

## 6. Disclosures

- **Undone sweep commit**: one `but commit` ran without explicit ids and
  swept the historical uncommitted pool into commit `mkl` (sha a9d60dc5);
  undone immediately via `but undo`, redone as `kkz` with allowlist `rl krv`.
  Recorded per the no-silent-repair rule (ADR-0083 D-C).
- **adr-0068 flake**: `registration precedes collection` leg failed once
  inside the battery (empty `git log` read, transient - concurrent `but`
  background sync); passes standalone and in the terminal wave. Disclosed,
  not masked.
- **Regen-boundary**: rewrite-map `--check` legs red in waves 1-2 while
  citation-bearing commits were in flight; terminal wave green; a post-seal
  regen commit resyncs the map over the claim+SEAL commits (same disclosed
  shape as grill-t26).
- **UNVERIFIABLE column**: 4 ci-mode gates degrade locally (registered
  semantics; they require CI mode, not the corpus). In CI post-refresh the
  corpus gates run for real; absent/invalid secret -> UNVERIFIABLE +
  `::error`, never silent skip.
- Intermediate commits may be red (t12 O-B); the round-final state is green.
- No atomcode research needed (mature zero-dep path chosen over research).

## 7. Acceptance criteria status

- compile/build equivalent: `node --check` on all touched sources + full
  jest (1367/1367) + `gate:all` EXIT 0.
- pack: `check-pack-smoke` EXIT 0 (353706 B, budget pass).
- process liveness: tarball-extracted `install.js --help`/`init --dry-run`
  + `jiahao-mcp` JSON-RPC initialize -> JSON-RPC result, EXIT 0.
- per-platform test loop: sentinel D2b deterministic on POSIX+Windows
  (rename-over); npm-CLI leg probes both Node layouts + PATH; restore
  rehearsal is tar-implementation-agnostic (basename argv under cwd, works
  with bsdtar + GNU tar).

## 8. Human-side tail (owner-only, none executed by agent)

1. `gh secret set JIAHAO_BENCH_CORPUS_B64 < .scratch/.tmp-t27-secret-refresh/JIAHAO_BENCH_CORPUS_B64.txt`
2. Merge `grill-t27` + `grill-t27-docs` to main (pushes are owner-side).
3. First green `origin/main` run -> execute defer-0070 seven-field closure.
4. Optional `adjudicated/grill-t27` tag co-naming the SEAL sha.
