# grill-t27 round handoff (2026-09-25)

Lane `grill-t27` is closed at the terminal wave (all legs EXIT 0 at
a1776dde; claim commit + SEAL + post-seal regen ride on top). What is left
is owner-side authority work plus one event-driven closure.

## State at handoff

- 6 CI-red legs triaged + fixed: restore-step validate-or-absent (D-002),
  sentinel rename-over implant (D2b), npm dual-layout probe (D5).
- defer-0072 registered as the degrade successor tracker; defer-0071
  untouched; defer-0070 still `pending-evaluation` awaiting the first
  green origin/main run.
- Secret payload prepared (not written): `.scratch/.tmp-t27-secret-refresh/
  JIAHAO_BENCH_CORPUS_B64.txt`; owner doc `.scratch/grill-t27/
  secret-refresh-package.md`.
- Report: `.scratch/grill-t27/reports/2026-09-25-report.md`; raw numbers
  in `round-facts.json`; measurement definitions/verdict in ledger D-009.

## Owner actions (exact order)

1. `gh secret set JIAHAO_BENCH_CORPUS_B64 < .scratch/.tmp-t27-secret-refresh/JIAHAO_BENCH_CORPUS_B64.txt`
2. Merge `grill-t27` + `grill-t27-docs` into main (push is owner-side).
3. Wait for the first `origin/main` workflow run; on `conclusion=success`
   execute the defer-0070 seven-field closure (template below).
4. Optional: `git tag -a adjudicated/grill-t27 <seal-sha>` - annotated tag
   MUST contain the bare seal sha in its message (co-named semantics).

## defer-0070 closure procedure (event-driven, seven fields)

On the first origin/main run with `conclusion=success`:

```
{
  "run_id": "<gh run list --branch main --limit 1>",
  "conclusion": "success",
  "verifiable_composition": "<which corpus gates ran vs UNVERIFIABLE - read the run log>",
  "degradation_semantics": "exit-2 UNVERIFIABLE is a degrade verdict, not a pass/fail - record which column each gate landed in",
  "degradation_cause": "<none | stale-secret still degrading -> then this is a yellow-grade closure only>",
  "successor_defer_id": "defer-0072",
  "closure_rule_verbatim": "<quote the unfreeze_if.check text verbatim>"
}
```

If the run is green-but-degraded (corpus gates still UNVERIFIABLE): that is
a yellow-grade closure - the fix landed but the secret refresh is still
pending; defer-0072 stays open as the budget ledger.

## Resume hints

- Re-run the whole battery: `node .scratch/grill-t27/capture-battery.cjs`.
- Freshness self-eval prints all registered rounds' verdicts; SEAL + tag
  state appear under `grill-t27.seal`.
- Watch `but` background sync while jest runs git-history legs (adr-0068
  flaked once inside a battery; standalone + terminal wave clean).

## Suggested skills for the next session

- `$implement` (already the resident workflow), `$handoff` for the next
  closeout; `codegraph`/`ctx_search` for exploration; `atomcode-research`
  only if an external-tooling question resurfaces.
