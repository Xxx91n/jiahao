# grill-t7 impl-round REWORK — response to 2026-09-15 audit

Date: 2026-09-15 | Fix window: audit-driven rework | Commit: `kmu` on
`grill-t7-impl-round` (stacked above krs+kso)
Audit under response: `.scratch/grill-t7/reports/2026-09-15-audit.md`

## Dispositions

| Finding | Disposition | Evidence |
|---|---|---|
| **F-1** README counts stale → CI test job red | **FIXED** | README:187 `827 tests across 58 suites`; README:273 `58 test suites, 827 tests`; public-skip recount 4→10 (fullT sites: adr-0036×3, adr-0037×1, adr-0064-t1×3, adr-0065-impl×3; +1 pyT python-lane noted). `node scripts/run-test-gate.js --expected-suites 58` → `[test] OK: 58 suites, 827 tests, 0 skipped`, exit 0. Reconciliation of 787+34≠823: the suite is 38 tests (34 + 4 rework-added); baseline README figure was itself stale by 2 — both declarations now machine-checked green. |
| **F-2** second_reviewer never countersigned | **NOT LANDED — disclosed, authority boundary** | The seq-6 precedent shows `second_reviewer` is a human attestation (Xxx91n). An agent cannot manufacture a second reviewer. `defer-0042` keeps the slot open (review 2026-12-14); the countersign command path is `scripts/instrument.js` `criteria_change` (needs attestation_type `approve`, reviewer_id, second_reviewer, criteria_version `ADR-0066-300000`, previous `ADR-0062-230000`, restatement_of `<commit>`). **User action required.** |
| **F-3** 52/52 not reproducible | **FIXED (option a)** | `node scripts/collect-devin-corpus.js rescore` — repo-side spec interpreter (4 check types) replays every stored label from `spec`+`transcript`; output `[devin-corpus] rescore: agreement 52/52 (100%)`, artifact `bench/research/out/devin-rescore.json` committed. Exits 1 on any mismatch. |
| **F-4** replay log not persisted | **FIXED (option a)** | `check-g6-publish.js` writes `bench/research/out/g6-publish-replay.json` every run (deterministic: extracted-surface sha256, per-tier maxima, control verdict, no clock fields). Verified: `20/20` bit-equal, `logit_diff_max 2.66e-14 < 1e-12`, `corrupted_manifest_rejected: true`. |
| **F-5** count-band advisory unimplemented | **FIXED** | `bandAdvisory()` in collect-devin-corpus.js: emitted on `validate` (frozen+pending) and `snapshot` (merged count) vs `plan.target_band`; WARN-only, never an exit code. Wiring test covers in-band/out-of-band. |
| **F-6** doc rot | **FIXED** | `g6-manifest-word1.json` _doc + `export_manifest.py` now cite `src/port/score.js`; `corpus-classes.json` devin-truth `frozen:true` AND the flag is now gated against live manifest status (check-corpus-classes.js lockstep check — it can no longer drift silently); devin manifest `source_adr` aligned to `0064/0065` matching plan.json. |
| **F-7** pyJsonNum `1e+021` | **FIXED** | Integral `|v|≥1e21` now expands to Python int digits (`pyJson(1e21) === '1000000000000000000000'`); unit-tested. The float lane already padded `e±d` correctly. |
| Smells (recorded) | partially folded | `mockRestore` moved into `finally`; determinism assertion added. Tiered-replay shape ×3 (compare/replay/goldensCheck) left as-is: they check different tier sets per context — recorded as an accepted judgment call. `fullT` naming kept (consistent with sibling suites). |

## Mandatory re-run checklist (all re-executed this window)

1. `npx jest` → **58 suites / 827 tests, all pass** ✓
2. `node scripts/run-test-gate.js --expected-suites 58` → **exit 0** ✓ (the previously-missed wrapper)
3. `npm run gate:all` → **exit 0, 29 entries, 4 ci-mode UNVERIFIABLE** ✓
4. `npm pack --dry-run --json` → **273,240 B / 101 files < 300,000** ✓
5. `check-pack-smoke.js` `check-g6-publish.js` `check-corpus-classes.js` `collect-devin-corpus.js validate` `confirmatory.js` → **all exit 0** ✓
6. `install.js --help` / `init --dry-run -y` → **exit 0** ✓
7. `npx jest test/adr-0065-impl-wiring.test.js` → **38/38** ✓ (34 → 38: rescore, band, pyJsonNum, replay-artifact tests added)

## Process note

- The rework itself touched a packed file (score.js), so pack M moved
  270,813 → 273,240. Per the pinned re-measure rule the trend row is
  appended (ADR-0039 row 10); the 300,000 cap derivation is anchored at the
  amendment-time M per ADR-0066 — recorded in its post-amendment note.
- Report claims audited: every figure above is now backed by a committed
  artifact or a rerunnable command (52/52 → `rescore` + rescore.json;
  replay log → g6-publish-replay.json).
- The `.scratch` collector (`devin-collect.js`) remains the session harness;
  the rescore logic now lives repo-side in `scripts/collect-devin-corpus.js`
  (CHECKS + rescoreLabel) so the agreement figure no longer depends on a
  gitignored file.
