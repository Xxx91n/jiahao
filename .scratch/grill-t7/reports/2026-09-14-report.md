# T-6 confirmatory implementation round - evidence report

Date: 2026-09-14 | Agent: fix/dev sub-agent | Slug: grill-t7
Branch: grill-t7-impl-round (stacked on grill-t7-doc-round; commits krs + kso)
Scope: next-round.md T-1..T-5; ledger D-001..D-006; ADR-0064/0065/0066.

## Verdict

CONFIRMATORY PASS. The shipped product port replayed the frozen corpus above
the pre-registered floor; no fallback needed. Every claim carries a
rerunnable command + observed output.

## T-1 product port (D-003)

- `src/port/score.js` exports pure `score(text) -> {logits, verdict}` plus
  the gate seams (tokenize/vectorize/logit/itemText). Purity: no
  fs/net/process access; the only require is `./g6-manifest.json` (asserted
  by the wiring suite).
- Manifest ships inside src/: `src/port/g6-manifest.json` (vocab 2696,
  float64 coef/intercept, corpus fingerprints pinned).
- Old copies removed: `bench/research/sklearn-port.js`,
  `bench/research/g6-manifest.json` deleted.
- Evidence: `npx jest test/adr-0065-impl-wiring.test.js` -> 34/34 pass,
  including the purity source scan and the corrupted-manifest positive
  control (intercept bump changes logits).

## T-2 confirmatory bench (D-002)

- Command: `node bench/research/confirmatory.js`
- Output: `[confirmatory] char-3|count|lr|C1.0|df2: recall@FP0 1.000000 vs
  floor 0.563863135740939; fp_default 0.000000 vs margin 0.045 -> PASS`
- Artifacts: `bench/research/out/confirmatory-result.json` (verdict PASS,
  judged_first char-3, fallback.fired=false) and
  `bench/research/out/confirmatory-report.md`.
- Fallback leg implemented (not fired):
  `bench/research/g6-manifest-word1.json` exported via
  `export_manifest.py --survivor-config-id "word-1|count|lr|C1.0|df2"`;
  the leg is exercised by a tampered-floor wiring test (fallback.fired=true,
  judged at the same floor, FAIL reported honestly).
- Live replay also runs inside the wiring suite (corpus-gated, full-tier).

## T-3 G6 publish gate (D-003)

- `scripts/check-g6-publish.js` wired through package.json
  `prepublishOnly`; registered `docs/gates.json` `[153 g6-publish]`
  confirmatory, source_adr 0065 (_doc cites 0064/0065).
- Fixture `bench/research/g6-publish-fixture.json`: gold20 whole-file
  sha256 `3a727fd3...` + per-item tokens_sha256/logit (no item text).
- Evidence: `[g6-publish] OK: jiahao-0.0.1.tgz replayed 20 gold items from
  the extracted tarball - token digests bit-equal (a), rel-L2 advisory (b),
  logits < 1e-12 (c); corrupted-port positive control rejected`.
- Local pack unaffected: `prepublishOnly` does not fire on `npm pack`.

## T-4 devin-corpus@v1 (D-004)

- `plan.json` committed FIRST (commit krs) before any item.
- Collection: `.scratch/grill-t7/devin-collect.js` ran REAL executions
  (file writes + reads), 3 drops: 20 honest + 20 honest + 12 lie = 52
  items; incoming drops consumed by the single snapshot.
- Mechanical labels: dual verification (spec-layer + judgment) on identical
  transcripts; 52/52 (100.0%) agreement; no kappa (mechanical labels).
- `manifest.json`: status frozen, harness_commit 8d04539314cd..., model
  devin-desktop 1.126.0, collected_at 2026-09-14, disjoint_from pinned,
  blind_until rung-1 settlement, explicit conformity_disclaimer.
- Evidence: `node scripts/collect-devin-corpus.js validate` -> 3 pending
  drops; `snapshot` -> frozen 52; `node scripts/check-corpus-classes.js`
  -> OK: four classes distinct, pinned, disjointness rules armed.

## T-5 claims + governance (D-006)

- `bench/research/out/claim-template.md` carries 6 fixed facts; the
  identical block is repeated in `confirmatory-report.md` and `README.md`
  (whitespace-normalized verbatim, asserted by the wiring suite).
- defer-0042 (ADR-0066 review slot), defer-0043 (net-addition tally +1),
  defer-0044 (terminal event PASS) registered pending-evaluation; all three
  referenced in ADR-0066/0065 text; trend-inventory untouched
  (documentation rounds only, per ADR-0064 D-F).
- README ADR index rebuilt (66 entries); ci.yml expected-suites 57 -> 58.

## ADR-0066 cap amendment

- Packing the manifest pushed the tarball past the 230,000 cap. Fixed per
  ADR-0062: trend rows 7-9 appended (226,867 / 269,320 / 270,813), live cap
  300,000 = `ceil_to_10_000(270,813 x 1.10)`; defer-0042 review 2026-12-14.
- Evidence: `npm pack --dry-run --json` -> 270813 bytes, 101 files;
  `node scripts/check-pack-smoke.js` -> `budget 270813 < 300000`, exit 0.

## Verification closure

- `npx jest`: 58 suites / 823 tests all pass.
- `npm run gate:all`: exit 0, 29 entries (4 UNVERIFIABLE = ci-mode-only
  gates, fail-closed semantics preserved); new gates 151
  confirmatory-bench + 153 g6-publish both PASS inside the run.
- Process liveness: `node scripts/install.js --help` -> usage + exit 0;
  `node scripts/install.js init --dry-run -y` -> plan + exit 0 (CLI
  process starts and answers; one-shot installer surface).
- Code review: two-axis review (standards + spec) run; findings folded in:
  phantom usage flags removed, relL2 deduplicated to the
  check-g6-equivalence export, checkG6 warnings-drop bug fixed, fallback
  leg made real, provenance concat bug fixed, D-006 registry rows added.

## Honesty notes

- rel-L2 measured 0/0 (count weighting = exact integer arithmetic on both
  sides) - advisory channel, never a gate input.
- Confirmatory replay is in-sample by construction (artifact trained on the
  full frozen corpus, deterministic split); the out-of-fold claim remains
  the rung-1 research number - stated verbatim in every claim home.
- devin-corpus@v1 is a ground-truth corpus; it is never cited by any
  conformity claim.
