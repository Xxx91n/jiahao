# grill-t7 unblind round - execution report (2026-09-15)

Branch: grill-t7-unblind-exec (stacked on grill-t7-unblind-docs over 94f56dd).
Commits: vts (adr-0060 stale-assertion fix) -> zmr (T-1 pre-registration) ->
xqz (T-2 runner+tests) -> skm (T-3 single-shot artifacts) -> mkn (T-4 claim
surface) -> upk (T-5 replay gate + terminal row). No push, no PR.

## Verdict (single-shot, burned)

devin-corpus@v1 falsification test: indeterminate (n=52, lie=12, CI lower
0.054861) (verdict date: 2026-09-15)

This is a small-sample (n_lie=12) decision-table outcome, not a precise
performance estimate; devin-corpus@v1 is never cited by any conformity claim.

The pre-registered integer decision table assigns 3/12 to the indeterminate
band; this is a decision-table outcome, not an effect estimate. FP@default
10/40 (0.25) - the descriptive guardrail is not observed; no FP
non-inferiority claim is made. All 3 hits and all 10 FPs sit in the
exit-report check shape; drop_closing delta is 0 on this OOT set.
Branch policy armed: INDETERMINATE -> devin-corpus@v2 (n_hon~100), registered
before v2 data with the designed-after-v1 disclosure (defer-0046).

## Evidence (rerunnable)

- `node bench/research/devin-oot.js --validate` -> "52 items serialized,
  0 defects, positive control OK (labels untouched)" (ran BEFORE the single
  shot; label/scoring_function never touched on this path).
- `node bench/research/devin-oot.js run` -> "indeterminate - k=3/12 CI
  [0.054861,0.571858] floor 0.563863 | recall@default 0.250000 FP 10/40"
  (single execution; the runner now refuses re-run with exit 1 +
  `[config]: REFUSED` - see the audit-rework note below).
- `node bench/research/devin-oot.js --replay` -> "OK: stored artifact
  re-derives cleanly (indeterminate, k=3/12, CI lower 0.054861)".
- `npx jest --silent` -> 59 suites / 865 tests, 0 failures.
- `npm run gate:all` -> "exit 0 (30 entries, 4 unverifiable: ci-mode
  absent locally)"; [154 devin-oot-replay] PASS inside it.
- `npm pack --dry-run --json` / pack-smoke -> 275,397 B < 300,000 B cap
  (ADR-0039 D3), src/port + manifest packed, bench/ excluded.
- `node scripts/install.js --help` + `init --profile verifier --dry-run`
  -> usage + dry-run plan printed (CLI liveness).
- `git diff HEAD -- bench/research/devin-corpus/manifest.json` -> empty
  (corpus manifest untouched; settlement is report-side).

## Pre-existing defect repaired en route

Commit dadc50d (docs branch, ADR-0066 countersign) appended a seq-13
criteria_change event that broke adr-0060-wiring's STD-1 tail assertion
(the invariant wanted the repaired sign-off to be the live tail, not the
last event of ANY kind). Scoped the assertion to the conditional_signoff
kind; suite green. Committed separately as vts before T-1.

## Corrections inside the round

- eval-plan.json rule_of_three formula text fixed (1-alpha^(1/n)) and amended
  into the T-1 commit; no registered parameter changed.
- gates.json params must declare --replay (ADR-0036 D4); capability require
  anchored in devin-oot.js (ADR-0040 D7d).

## Audit rework (2026-09-15, audit-unblind findings)

Independent audit (.scratch/grill-t7/reports/2026-09-15-audit-unblind.md)
returned NOT CLEAN PASS. Resolutions:

- F-1 (hard): the refusal exit was moved 2 -> 65, inside the R1-rejected
  sysexits band. Corrected to the ADR-0041 D3 contract: exit 1 with
  closed-enum prefixes - `[config]: REFUSED` (state refusal),
  `[config]: DEFECT`/`FAIL-CLOSED` (abort path), `[config]: FAIL`
  (replay drift), `[usage]:` (bad/missing args). Wiring asserts status 1
  + the prefix regex. The earlier "2->65 is the fix" framing was a
  claim-vs-standard mismatch; this note replaces it.
- F-2: the corrupted-manifest positive control now scores the real
  per-item itemText (rows retain `text` in memory; the artifact schema is
  unchanged). The stored artifact's positive_control stays accurate:
  intercept +10 moves all 52 serialized logits.
- F-3: a defect abort now persists `run_status=aborted` + defect list to
  devin-oot-report.json (never a verdict) per the registered wording; a
  completed artifact still mechanically refuses re-run.
- F-4: claim-context scope named in ADR-0067 D-C (amended into the T-1
  commit); wiring tightened to per-mention binding: every devin-corpus
  mention in the three claim homes is either inside the fact-line section
  or matches a registered exemption category (index links, prohibition
  bullet, title/metadata, settlement path refs). eval-plan.json bytes stay
  frozen - its sha256 is anchored in the stored report; the scope
  clarification lives in the ADR.
- Smells folded in: no-arg invocation errors `[usage]:` (run must be
  explicit), refusal precedes any corpus read, duplicate-id no longer
  double-flags malformed items, root threading unified, test split regex
  matches the runner (`/\r?\n/`).
- P-3 disclosure: "exactly one execution" is not machine-provable
  (delete-artifact + rerun leaves no trace); the guarantee is commit order
  + the mechanical refusal + this disclosed bound.

Re-run after rework: jest 59/865, gate:all exit 0 (30 entries),
validate/replay/refusal all green under the new contract.

## Artifacts

- bench/research/devin-corpus/eval-plan.json (pre-registered, frozen)
- docs/adr/0067-devin-corpus-v1-oot-falsification-adjudication.md
- bench/research/devin-oot.js (runner: --validate / run / --replay)
- bench/research/out/devin-oot-report.{json,md} (the settled artifact)
- bench/research/out/claim-template.md + README.md (bound claim block)
- docs/gates.json (devin-oot-replay, order 154)
- docs/deferred-registry.json (defer-0045 net-addition, defer-0046 terminal)
- test/adr-0067-wiring.test.js (37 assertions: seeds+runner+claim+closure
  + exit-contract/abort-artifact/per-mention rework coverage)
