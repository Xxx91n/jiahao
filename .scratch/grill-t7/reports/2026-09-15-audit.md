# grill-t7 impl-round AUDIT — independent verification report

Date: 2026-09-15 | Auditor: audit agent (this window) | Scope: `grill-t7-doc-round...grill-t7-impl-round`
(commits dc9d4e3 plan-first + c1a5ab8 T-6 round) | Report under audit: `.scratch/grill-t7/reports/2026-09-14-report.md`

## Verdict

**NOT CLEAN PASS — 打回返工 (1 hard defect + 1 disclosed procedure deviation + 3 spec/evidence gaps).**
All hard-acceptance commands re-ran green and ~25 of 26 key report claims verified against repo
artifacts, but one committed defect makes the next CI test job red, and three spec-letter gaps
survive. Fix list + mandatory re-run checklist at the bottom.

## 1. Hard acceptance re-run (auditor-executed, this machine, full corpus tier)

| Command | Report claimed | Auditor observed | Result |
|---|---|---|---|
| `npx jest` | 58 suites / 823 tests pass | `Test Suites: 58 passed`, `Tests: 823 passed`, exit 0 | PASS |
| `npm run gate:all` | exit 0, 29 entries, 4 UNVERIFIABLE ci-mode-only | exit 0, `29 entries, 4 unverifiable` (ci-wiring/bench-gate/probes/mr-probes), gates 151+153 PASS in-run | PASS |
| `npm pack --dry-run --json` | 270,813 B / 101 files | `[{"name":"jiahao","size":270813,"files":101}]` | PASS |
| `node scripts/check-pack-smoke.js` | `budget 270813 < 300000` exit 0 | identical output, exit 0 | PASS |
| `node bench/research/confirmatory.js` | PASS line verbatim | `recall@FP0 1.000000 vs floor 0.563863135740939; fp_default 0.000000 vs margin 0.045 -> PASS`; artifacts unchanged | PASS |
| `node scripts/check-g6-publish.js` | OK replay + control rejected | identical OK line, exit 0 (packs to scratch, never blocks local pack) | PASS |
| `node scripts/install.js --help` / `init --dry-run -y` | usage + plan, exit 0 | both exit 0, usage/dry-run plan printed | PASS |
| `npx jest test/adr-0065-impl-wiring.test.js` | 34/34 | `Tests: 34 passed, 34 total` | PASS |
| `node scripts/collect-devin-corpus.js validate` | 3 pending drops (pre-snapshot) | `52 frozen items, 0 pending drops` — consistent post-snapshot state | PASS (state evolved) |
| `node scripts/check-corpus-classes.js` | OK four classes | identical OK, exit 0 | PASS |
| `node scripts/run-test-gate.js --expected-suites 58` | (not run by the round) | `FAIL: README (Develop) declares 787 tests / 57 suites, actual 823 tests / 58 suites` | **FAIL — see F-1** |

## 2. Claim → evidence → conclusion (key claims)

| # | Report claim | Auditor evidence | Conclusion |
|---|---|---|---|
| C1 | `src/port/score.js` pure `score(text)->{logits,verdict}` + gate seams | file exists (7,938B); only `require('./g6-manifest.json')`; exports tokenize/vectorize/logit/score/scoreItem/itemText/pyJson/pyStr; wiring purity scan passes | PASS |
| C2 | manifest in src/, vocab 2696, float64, fingerprints pinned | `src/port/g6-manifest.json` 117,054B; coef len 2696 w/ full-precision decimals; `corpus_fingerprints` == thresholds.json pins (067a837f…/65dcd665…) | PASS |
| C3 | old copies deleted | `bench/research/sklearn-port.js`, `bench/research/g6-manifest.json` both absent | PASS |
| C4 | fallback leg real (word-1 manifest, tampered-floor test) | `g6-manifest-word1.json` 37,132B, `ported_from: g2-survivor:word-1…`; wiring test fires fallback via mocked floor → verdict FAIL, fallback.fired=true | PASS |
| C5 | gates.json `[151 confirmatory-bench]` `[153 g6-publish]` confirmatory, source_adr 0065 | both registered, tier confirmatory, source_adr = 0065 path; `_doc` cites 0064+0065. Ledger D-003 literal said `0064`; ADR-0065 says `0064/0065` — deviation resolved by the ADR, gate `_doc` carries both | PASS (deviation noted, F-6 class) |
| C6 | fixture: gold20 sha256 3a727fd3…, 20 items, no item text | `gold20_sha256` matches; 20 expectations `{id, tokens_sha256, logit}`; zero text/token fields; ids `devin-/pb-` only | PASS |
| C7 | plan.json committed FIRST before items | `git show dc9d4e3 --name-only` = plan.json only; dc9d4e3 is parent of c1a5ab8 | PASS |
| C8 | 3 drops 20+20+12=52, single snapshot consumed | incoming/*.consumed line counts 20/20/12; 0 pending .jsonl; items.jsonl = 52 | PASS |
| C9 | mechanical labels, dual verification 52/52 (100%), no kappa | labels 40 honest/12 lie via deterministic scoring_function per item; `no kappa` declared in manifest+plan+harness; **but the 52/52 figure has no committed artifact or rerunnable command — the re-scorer (`CHECKS`/`verifyItem`) lives in gitignored `.scratch/grill-t7/devin-collect.js`; repo `validateItem` checks schema only, never re-derives labels** | PARTIAL — F-3 |
| C10 | manifest.json: frozen, harness_commit, model, date, disjoint_from, blind_until, disclaimer | all fields verified verbatim (`8d04539314cd…`, `devin-desktop 1.126.0`, `2026-09-14`, 3 disjoint lists, `blind_until: rung-1 settlement`, never-cited disclaimer) | PASS |
| C11 | claim-template 6 facts verbatim in confirmatory-report + README | all 6 normalized fact strings contained in both files (auditor re-ran the test's extraction) | PASS |
| C12 | defer-0042/0043/0044 pending-evaluation, referenced in ADRs | registry rows verified; ADR-0065 same-commit amendment (+4 lines) records the writes; review_at 2026-12-14 on defer-0042 | PASS |
| C13 | trend-inventory untouched | `docs/governance/trend-inventory.json` rounds=2, all `kind: documentation`, no impl-round row | PASS |
| C14 | README ADR index rebuilt (66) | 66 unique `docs/adr/NNNN` refs in index block | PASS |
| C15 | ci.yml expected-suites 57→58 | diff verified (`--expected-suites 58`) | PASS |
| C16 | ADR-0066 cap amendment per ADR-0062 (rows 7-9: 226,867/269,320/270,813; cap = ceil(270,813×1.10)=300,000; review 2026-12-14) | all literals present in ADR-0066 + ADR-0039 anchor amended; arithmetic correct; policy-before-value + not-retro declaration + defer-0042 registered | PASS on artifacts; **second_reviewer instrument event never landed** — F-2 |
| C17 | two-axis code review ran, fixes folded in | fix evidence present in diff (relL2 dedup to check-g6-equivalence export, warnings channel restored, real fallback leg, provenance concat fix, registry rows) | PASS (fix artifacts verifiable; the review act itself is report-asserted) |
| C18 | "every claim carries a rerunnable command + observed output" | true for all but: the 52/52 figure (F-3) and "validate → 3 pending drops" (state-dependent, now 0) | PARTIAL |

## 3. D-001..D-006 implementation-evidence check

- **D-001 (scope)** — diff strictly inside T-1..T-5 + ADR-0066; no side-topics (release channel, defer-0040) touched. PASS.
- **D-002 (adjudication)** — single absolute floor from frozen `mde-freeze.json` (0.4792+0.08466313574093907 ≈ 0.563863135740939, self-consistency throw on tamper); ladder capped at the two survivors; fallback same floor + mandatory honesty sentence; rel-L2 → warnings channel never exit code; poisoned-idf positive control; truthful-FAIL path rendered. All machine-asserted, auditor re-ran. PASS.
- **D-003 (port surface)** — manifest in src/ (files whitelist unchanged), float64 no quantization, pure zero-dep scorer, prepublishOnly-only publish gate (verified `npm pack` does not fire it; gate's own pack goes to scratch dir), sha256 fixture, corpus doors out of npm surface. PASS.
- **D-004 (devin corpus)** — plan-first commit, band [40,60] (52 landed), 3 validated drops → exactly one snapshot, metadata readable / labels blind to rung (no rung-stat consumer found: only check-corpus-classes + collect script touch the dir), disclaimer + disjointness armed. PASS except F-3 (52/52 evidence) + F-5 (count-band advisory unimplemented, latent).
- **D-005 (closure)** — tests green + gate:all exit 0 + wiring green all re-verified; artifacts: confirmatory-report.md ✓, manifest.json provenance ✓, **G6 publish-gate replay log never landed on disk** — F-4.
- **D-006 (claims+governance)** — registry rows correct (net-addition +1 = defer-0043, terminal PASS = defer-0044, no fallback/count-band/rel-L2 rows), claim-template is single authority, verbatim repetition machine-checked, tier-1 limitation sentence carried in fact 4 across all three claim homes. PASS.

## 4. Findings (打回返工清单)

### F-1 [HARD / CI-breaking] README declared counts stale → test job red
- `README.md:187` `787 tests across 57 suites … public tier skips 4 corpus-bound tests`; `README.md:273` `58 test suites, 820 tests`. Actual: 823/58.
- `scripts/run-test-gate.js` (ci.yml test job, ADR-0057 D-C) machine-checks BOTH declarations against the JUnit header — auditor ran it: `FAIL: README (Develop) declares 787 tests / 57 suites, actual 823 / 58`. The round's own evidence (`npx jest`, `gate:all`) never covered this wrapper — it is CI-only by design (ADR-0058 R8, no gates.json entry).
- Fix: update both README lines to real counts and recount the public-tier skip figure (full-tier-gated call sites are now 10: adr-0036×3, adr-0037×1, adr-0064-t1×3, adr-0065-impl×3 — recompute honestly). Also note 787+34=821 ≠ 823 — reconcile where the other +2 tests came from.

### F-2 [procedure deviation, disclosed — user decision required] ADR-0066 second_reviewer never countersigned
- ADR-0062 closed its own amendment via instrument `criteria_change` (seq 6); `src/instrument-state.json` ends at seq 12 — no criteria_change event exists for ADR-0066. ADR-0066 D-E discloses the slot OPEN + defers countersign to "the next audit round" (the ADR-0065 ratification pattern); defer-0042 tracks it; the ledger ratified it user-side.
- This audit round IS the deferred target. Auditor does not ratify — the countersign decision is the user's (or the fix window's explicit task).

### F-3 [evidence gap] "52/52 (100.0%) agreement" not reproducible
- No committed artifact or command produces the figure; the mechanical re-scorer lives only in gitignored `.scratch/grill-t7/devin-collect.js`. Violates the report's own standard ("every claim carries a rerunnable command + observed output") and weakens D-004(b) "记 provenance" to a method declaration rather than a recorded result.
- Fix options: (a) land a repo-side re-score/verify subcommand (or a `.scratch` evidence log like prior rounds kept under `evidence/`) producing the agreement count; (b) downgrade the report claim to what manifest/plan actually assert.

### F-4 [spec letter] G6 publish-gate replay log not persisted
- D-005(b) names the T-3 closure artifact as "回放日志" landed on disk. `check-g6-publish.js` prints to stdout and deletes its scratch dir; `run-gates.js` persists nothing; the only record is this gitignored report.
- Fix options: emit an artifact (e.g., `bench/research/out/g6-publish-replay.json` or evidence-log append), or amend the spec wording to declare gate:all stdout the log of record.

### F-5 [latent] count-band advisory declared but unimplemented
- `plan.json` declares count-band misses → advisory warning; no code emits it (52 is in-band so unexercised). Implement or explicitly defer-register.

### F-6 [minor doc rot / inconsistencies]
- `bench/research/g6-manifest-word1.json` `_doc` and `bench/research/export_manifest.py` still cite deleted `bench/research/sklearn-port.js`.
- `bench/research/corpus-classes.json` `devin-truth: "frozen": false` vs `manifest.json status: "frozen"` (ungated field, drifts silently).
- `manifest.json source_adr: "0064"` vs `plan.json source_adr: "0065"` — mirrors the 0064/0065 split but inconsistent within one snapshot.

### F-7 [latent edge] `src/port/score.js` pyJsonNum
- `String(v).replace('e+','e+0')` for |v| ≥ 1e21 yields `'1e+021'` — matches neither Python (`1e+21`) nor JS. Unreachable for current corpus values; flag for the next port-surface touch.

### Judgement-call smells (non-blocking, record only)
- Duplicated Code: tiered replay shape ×3 (`compare` in check-g6-equivalence, `replay` in check-g6-publish, `goldensCheck` in confirmatory) + identical corrupt-manifest block + triplicated helpers — despite the claimed relL2 dedup.
- Mysterious Name: `fullT` in adr-0065-impl keys off tmp-clone `corpusPresent`, same name as CORPUS_TIER==='full' gate elsewhere — different semantics.
- Test hygiene: `spy.mockRestore()` not in `finally` (adr-0065-impl :97-103, unlike :165-178); `verdict === logits>0` assertion tautological (:47).
- Cosmetic: gates.json new rows appended after order-198 (runner sorts); WARN(advisory)/WARN(diagnostic) prefix split; `.scratch/g6-publish` scratch survives a mid-pack throw.
- Observation: `confirmatory.js` self-clones the pinned corpus from GitHub inside a `repo-tree` gate (network dep; disclosed in the entry `_doc`; bench-gate precedent) — defensible, noted.
- Observation: workspace carries an unrelated `g0` stack commit (`ignore test-artifacts/`) — parallel work, outside scope, untouched.

## 5. 过程违规呈报 (process violations — reported, not ratified)

- **P-1**: Round closure evidence skipped the CI test-job wrapper (`run-test-gate`), letting F-1 ship green locally but red on CI. D-005(b) requires per-step closure signals; "tests all green" was demonstrated only via plain `npx jest`, not the asserting wrapper the repo built for exactly this drift class.
- **P-2**: F-2's second_reviewer deferral is disclosed in ADR-0066 status/D-E and tracked by defer-0042 — deviation from ADR-0062-as-written (no deferral provision there), but openly registered; countersign authority is the user's.
- **P-3**: F-3/F-4 are claim-vs-artifact mismatches: the report states measured/persisted facts where only declarations or stdout exist. Not dishonest — insufficiently evidenced per this repo's own evidentiary bar.
- No evidence of assertion-weakening: all edited wiring tests (adr-0033/0038/0060/0064-t1) either extend exact-match inventories or re-point anchors required by ADR-0066; the one suspect hunk (adr-0064-t1 `['collecting','frozen']` tolerance) predates this diff.

## 6. Rework requirements for the fix window

Must-fix: **F-1** (README counts + public-skip recount).
Fix-per-decision: **F-2** (user countersign call), **F-3** (evidence or downgrade), **F-4** (artifact or spec clarification), **F-5** (implement or defer-register).
Optional: F-6, F-7, smells.

Mandatory re-run checklist after any fix (same acceptance + the missed wrapper):
1. `npx jest` → 58/823 (or updated real counts) all pass
2. `node scripts/run-test-gate.js --expected-suites 58` → **must exit 0** (new)
3. `npm run gate:all` → exit 0, 29 entries, 4 ci-mode UNVERIFIABLE
4. `npm pack --dry-run --json` → ≤300,000 B
5. `node scripts/check-pack-smoke.js`, `check-g6-publish.js`, `check-corpus-classes.js`, `collect-devin-corpus.js validate`, `bench/research/confirmatory.js` → exit 0
6. `node scripts/install.js --help` + `init --dry-run -y` → exit 0
7. `npx jest test/adr-0065-impl-wiring.test.js` → 34/34 (or updated count)

Audit performed read-only: no repo bytes modified; working tree clean (`git status` empty; run-test-gate wrote only the gitignored test-artifacts/junit.xml).

---

## 7. RE-AUDIT after rework commit `kmu` (d083014) — 2026-09-15 pass 2

Rework report under audit: `.scratch/grill-t7/reports/2026-09-15-rework.md`.
Same acceptance suite re-executed by the auditor — all green:

| Command | Observed | Result |
|---|---|---|
| `npx jest` | 58 suites / **827** tests, all pass (823 + 4 rework tests) | PASS |
| `node scripts/run-test-gate.js --expected-suites 58` | `[test] OK: 58 suites, 827 tests, 0 skipped`, exit 0 | **PASS — F-1 closed** |
| `npm run gate:all` | exit 0, 29 entries, 4 ci-mode UNVERIFIABLE | PASS |
| `npm pack --dry-run --json` | 273,240 B / 101 files < 300,000 | PASS |
| `install.js --help` + `init --dry-run -y` | both exit 0 | PASS |
| `npx jest test/adr-0065-impl-wiring.test.js` | 38/38 (34+4) | PASS |
| `collect-devin-corpus.js validate` | 52 frozen, 0 pending | PASS |

Per-finding re-verification:

- **F-1 FIXED** — README:187 `827 tests across 58 suites … skips 10 corpus-bound`; README:273 `58 test suites, 827 tests`; run-test-gate machine-checks both declarations green. (Note: 827 = 823 verified this audit + 4 new wiring tests; the earlier 787 figure was itself stale, consistent with the rework's reconciliation note.)
- **F-2 NOT LANDED — correctly deferred.** `src/instrument-state.json` history ends at seq 12 (last actor Xxx91n, human); no fabricated criteria_change. defer-0042 keeps the slot open; countersign is a human attestation, documented for the user. **Open item for the user, not a defect.**
- **F-3 FIXED** — `collect-devin-corpus.js rescore` replays all 52 stored labels repo-side: `agreement 52/52 (100%)`, exit 0; artifact `bench/research/out/devin-rescore.json` committed.
- **F-4 FIXED** — `check-g6-publish.js` persists `bench/research/out/g6-publish-replay.json` every run (committed; deterministic, no clock fields): extracted sha256, 20/20 bit-equal, logit 2.66e-14 < 1e-12, corrupted control rejected. D-005(b) replay-log closure artifact now exists.
- **F-5 FIXED** — `bandAdvisory()` wired into `validate` and `snapshot`; `WARN(advisory)` string, never an exit code; wiring covers in/out-of-band.
- **F-6 FIXED** — word1 manifest `_doc` + `export_manifest.py` now cite `src/port/score.js`; `corpus-classes.json` devin-truth `frozen: true` AND check-corpus-classes now gates frozen-metadata + item_count lockstep (cannot drift silently); devin manifest `source_adr: "0064/0065"`. (Cosmetic note: rework report says "matching plan.json" — plan is `0065`, manifest is `0064/0065`; substantively aligned, not byte-equal.)
- **F-7 FIXED** — `pyJson(1e21)` → `'1000000000000000000000'` (Python int digits); `pyJson(1e-7)` → `'1e-07'` preserved; unit-tested.
- Smells: mockRestore moved into `finally`, determinism assertion added; replay-shape ×3 and `fullT` naming recorded as accepted judgment calls — auditor accepts the recording (non-blocking by skill rules).
- kmu file set is inside rework scope only; no scope creep; no assertion weakened.

### Final verdict

**AUDIT PASS** (with one disclosed user-side open item: ADR-0066 second_reviewer countersign, defer-0042, review 2026-12-14).
The round's claims now each carry a committed artifact or a rerunnable command. Auditor ran every
acceptance command personally; working tree clean throughout.
