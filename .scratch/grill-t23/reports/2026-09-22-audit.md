# grill-t23 audit report - second-party verification of the cap-amendment + front-face round

- Date: 2026-09-22
- Auditor: verifier-profile second-party agent (post-round audit window over the grill-t23 round; no fixes performed - duty separation)
- Object under audit: the grill-t23 documentation round `688e113..HEAD` on GitButler branch `grill-t23-docs` (31 commits, tip `9b27a5e`, workspace wrapper `e77513e`): T-0 absorb/dispositions, T-1 cap amendment (ADR-0082 + ADR-0039 D3 340,000->380,000 + defer-0067/0068), T-2 brand matrix + pure-SVG system + bilingual README refresh + .github templates, T-3 gh metadata + social-preview guide, T-4 battery + disclosures + handoff - PLUS the two disclosed never-commit sweep/revert pairs (`a2e5571`/`837fca7`, `51a66d3`/`0e951ea`).
- Audit evidence (verbatim re-run output, uncommitted per the audit-evidence never-commit convention): `D:/Aworker/jiahao/.scratch/grill-t23/audit-evidence/` - 14 captures. Two-axis code review (Standards + Spec) ran as parallel read-only sub-agents over `git diff 688e113...HEAD`.
- Verdict: **PASS WITH FINDINGS** - every substantive claim re-verified live (suite green, pack under the amended cap, amendment channel conformant, no push), but nine nit-class findings (T4-C-1..C-9) route to the next round's T-0 dispositions. Two of them are in the same omission class the round itself backfilled this round.

## 1. Hard acceptance - independently re-run, not trusted from the round's own evidence

Canon under test: `.scratch/grill-t23/round-facts.json` = {suites 77, passed 1307, skipped 0, pack_bytes 340290, instrument_entries 27, rewrite_map_citations 2246, registry_entries 62, anchors_count 18, battery_as_of_commit 6e12154, report_commit null, not_run [bench-gate, ci-wiring, mr-probes, probes]}.

| Check | Claimed | Audit re-run | Verdict |
| --- | --- | --- | --- |
| `node scripts/run-gates.js` (gate:all) | exit 0, 35 gates | exit 0 - 35 entries, 4 UNVERIFIABLE (ci-wiring/bench-gate/probes/mr-probes, ci-mode absent), 3 advisories printed; pack-smoke 340,290 < 380,000 inside it | MATCH |
| `node scripts/run-test-gate.js --expected-suites 77` | 77/1307/0 | 77 suites / 1307 tests / 0 skipped, exit 0, 21.5s | MATCH |
| `node scripts/check-pack-smoke.js` | under amended cap | exit 0 - `340290 < 380000` (ADR-0039 D3) | MATCH |
| liveness (pack -> extract -> `install.js --help` -> `init -y --dry-run` -> MCP `initialize`) | exit 0 | all five steps exit 0; MCP handshake returns valid initialize result (protocolVersion 2024-11-05) | MATCH |
| `build-round-facts --round grill-t23 --check --report <report>` | in sync | exit 0 - "facts and report region in sync" | MATCH |
| `build-governance-anchors --check` | in sync | exit 0 - 18 artifacts in sync | MATCH |
| `build-rewrite-map --check` | in sync | exit 0 - 2,246 doc citations | MATCH |
| `check-deferred` | 62 entries OK | exit 0 - 62 entries (51 live, 11 closed/actioned) | MATCH |
| `check-governance-inventory` / `--coverage-base 688e113...` | OK + R2 declared | exit 0 both; both advisories printed (burn-rate 4-consecutive, trend-anchor streak 14) | MATCH |
| `check-governance-inventory --coverage-base not-a-real-ref` | keyed FAIL exit 1 | exit 1, keyed, no stack | MATCH |
| `check-ci-jobs` / missing-file | exit 1 / exit 2 | exit 1 (defer-0004 unmet, deferral valid) / exit 2 keyed ENOENT | MATCH |
| `instrument --check` | authoritative | exit 0 - state authoritative, conditional cert to 2026-12-11 | MATCH |
| `check-svg-assets.cjs` | 7 SVGs OK | exit 0 - 7 files, 0 failed | MATCH |
| jest adr-0080/0081/0082 wiring | green | 3 suites, 36 tests, exit 0 | MATCH |
| quoted-stale fixture (must-fail) | exit 1 | exit 1 - bare canon value + schema-key assign rejected | MATCH |
| `python build-brand-assets.py` + porcelain on docs/assets/brand/ | byte-identical | regen exit 0; `git status --porcelain` empty -> CLEAN | MATCH |
| clean-tree | CLEAN | 0 tracked diffs; 11 untracked all never-commit class | MATCH |
| h1-reread byte scan (independent re-scan) | 0 flagged | 308 committed .scratch/*.md + 137 *.txt, 0 flagged (counts grew past the committed capture's 306/112 - later commits added files; verdict stable) | MATCH |

Caveat (see T4-C-1): the *committed* run-test-gate.txt / round-facts.txt / rewrite-map.txt artifacts still carry battery-run-1 red bytes (EXIT 1 / 2 / 1). All three legs re-run green at HEAD under this audit; the table above reports the live re-run.

## 2. Report claims - claim -> evidence -> conclusion

| # | Claim (report / commit map / handoff) | Independent evidence | Conclusion |
| --- | --- | --- | --- |
| 1 | Facts canon block verbatim | every field re-measured live (jest, npm pack, map, registry, anchors); report sentinel region == round-facts.json | CONFIRMED |
| 2 | Branch grill-t23-docs, base 688e113, not pushed | `but status` shows the branch over common base 688e113; no GitHub remote exists (`gb-local` is the repo itself); nothing left the host | CONFIRMED |
| 3 | Commit map (25 sha rows + mixed-id rows) | all named commits resolve in `git log`/`but status` with matching subjects | CONFIRMED - cosmetic: rows mix sha and change-id tokens; `battery_as_of_commit 6e12154` appears in no row (T4-C-9) |
| 4 | ADR-0082: policy-before-value, M=339,408 pinned re-measure, `ceil_to_10_000(x1.10)=380,000`, not-a-retro declaration, second_reviewer -> defer-0068 tide | ADR-0082 read verbatim: D-A policy, D-B protocol, D-C value, D-E slot, Rejected section all present and ordered correctly | CONFIRMED |
| 5 | ADR-0039 D3 amended in place; live anchor single home | `docs/adr/0039` line 129 `out.size < 380,000 bytes`; amendment chain recorded (0062->0071->0082) | CONFIRMED |
| 6 | defer-0067 discharged-by-trigger; defer-0068 registered (2026-12-15 tide) | registry live: 0067 `closed`/`closed_via` names the amendment; 0068 pending-evaluation w/ review_at | CONFIRMED |
| 7 | Amendment landed before any surface-growing commit (defer-0067 ordering) | commit order: `d597c8a` (amendment) precedes `acf5388` (brand), `8effaaf` (README), `e308d8e` (.github) | CONFIRMED |
| 8 | Brand matrix 11 rasters, deterministic Pillow pipeline, byte-identical regen | 11 files present; IHDR dims verified (logo 1024, dark 1024, social-preview 1280x640, touch-icon 180, favicon-16 16); pipeline re-run -> porcelain empty | CONFIRMED |
| 9 | Pure-SVG system: hero + 2 diagrams + 4 badges, a11y metadata, no script/foreignObject | `check-svg-assets.cjs` exit 0 on 7 files; assets on disk | CONFIRMED |
| 10 | README preserve-refresh: failed verdict first-screen, measurement table zero-loss, local badges, mermaid replaced | failed v2 block verbatim at L23-36; verdicts-at-a-glance table + unchanged verbatim subsections; 4 local SVG badges; 0 mermaid / 0 shields.io | CONFIRMED |
| 11 | zh mirror same structure, baseline re-pinned | 11 `## ` sections identical order both files; `translation-baseline` comment = 8effaaf... = `git log -1 README.md` (drift pin green) | CONFIRMED - see T4-C-6 for the rhythm deviation |
| 12 | .github templates + ci.yml parity under declared carve-out | 4 template files + ci.yml landed; trend row gtd.files names all 5; ci.yml `--expected-suites 77` | CONFIRMED - nit: `config.yml` carries no inline disclosure comment (3/4 templates do) (T4-C-7) |
| 13 | gh repo edit verified live (description + 7 topics); social preview manual (no API) | `gh repo view` live: description + 7 topics present; `docs/assets/README.md` carries the upload guide; no fake API path | CONFIRMED - but no verbatim `gh` capture leg exists in the evidence set (T4-C-8) |
| 14 | Never-commit set intact end-state | committed tree holds only 5 pre-existing NC-pattern files (t7/t12/t13/t15, all present at base 688e113, untouched this round); both sweeps reverted; worktree bytes restored | CONFIRMED for the regex class - WEAKENED: `ref-assets/` was committed despite the task book's never-commit label (T4-C-2) |
| 15 | zero_product_diff true; no R1 touched | `git diff --name-only 688e113...HEAD`: non-docs/.scratch touches = 5 declared .github files + 13 `test/adr-*-wiring.test.js` (R3 via R3_WIRING_TEST) + g6-publish-replay.json (declared mechanism_output_diff); coverage leg exit 0 | CONFIRMED |
| 16 | Evidence set: verbatim `$ argv` + `EXIT n` | shape verified on all 25 committed captures; audit found 3 artifacts carrying run-1 red while table rows claim green (T4-C-1); h1-reread/liveness `$` lines are display-form but unmarked (T4-C-9) | CONFIRMED in shape - WEAKENED in content (3 stale-red artifacts) |
| 17 | "17 captures + 1 fixture" (split-form count) | committed evidence dir = 25 capture .txt + 1 fixture.md + 1 png; numeral off by 8 | MISMATCH (T4-C-4) |
| 18 | Consent sweep names full chartered set (0060/0064/0065/0066/0067/0068 + sunset + never-commit) | report sweep names 0067/0068/0060/0064/0065 + sunset + NC set; **defer-0066 absent** | PARTIAL (T4-C-3) - same omission class as T3-C-2 |
| 19 | T3-C-1/2/3 disposed | C-1: ADR-0081:40 dated in-place correction names all 3 R2 touches incl. thresholds.json; C-2: t22 ledger:21 dated backfill names the omitted lines; C-3: 'use strict' present at line 5 of both recapture scripts | CONFIRMED substance - PARTIAL form: no per-finding disposition table in the report (spec section 8) (T4-C-5) |
| 20 | Both never-commit sweeps + repairs disclosed | `a2e5571` diff = 5 clean-tree.txt swept, `837fca7` removes exactly those 5; `51a66d3` diff = 168 NC-class files swept, `0e951ea` removes them + `6e12154` purges tmp-swept.txt; worktree untracked copies present | CONFIRMED - disclosed and repaired; see section 6 |
| 21 | Mid-round reds disclosed | honest-state section enumerates them; the disclosed reds match the red artifacts found committed (run-1 state) | CONFIRMED in prose - the evidence table overstates the artifacts (T4-C-1) |
| 22 | Liveness/pack/test numbers in commit messages | spot-checked subjects vs diffs - consistent; exception: `51a66d3` claims "commit map tail rows" but its diff holds only the sweep + map regen (the rows landed in `8cf0625`/`9b27a5e`) | CONFIRMED with one mislabeled message (T4-C-9) |

## 3. Ledger conformance (D-001..D-007)

| D | Requirement | Evidence | Verdict |
| --- | --- | --- | --- |
| D-001 | a+b two-workstream round; amendment channel first; t22 absorb + nit dispositions; C-7 tick 2/2 | ordering verified (claim 7); absorb commit `657d422` carries both t22 audit artifacts; all three nits landed | CONFORMANT - C-7 registration line absent (T4-C-4) |
| D-002 | five-item full-spectrum front face | README pair + SVG system + brand assets + repo metadata + .github templates all landed | CONFORMANT |
| D-003 | pinned formula only; amendment before surface; not-a-retro declaration | ADR-0082 verbatim conformant (claim 4); no margin deviation | CONFORMANT |
| D-004 | preserve-refresh; verdict first-screen; zero fact loss; zh same-commit | claims 10/11 | CONFORMANT in substance - D6 same-commit letter broken once (T4-C-6) |
| D-005 | logo = jiahao meme via user-side GPT Image; Phase-6 checklist gates; other visuals pure SVG; static | brand/ assets + `evidence/logo-phase6-sheet.png` present; `docs/assets/README.md` records the external channel + derivation; 7 SVGs pass the gate | CONFORMANT |
| D-006 | gh metadata authorized; social-preview manual; NO push | gh metadata verified live; guide written; no GitHub remote exists | CONFORMANT |
| D-007 | assets under docs/assets/** (R3); .github carve-out 3 gates; dark variant programmatic halo; defer-0068 = second_reviewer slot; Phase-6 defects as input | all verified; burn-rate advisory printed and disclosed (advisory-only, correct per ADR-0076) | CONFORMANT |

## 4. Findings (route to next round T-0 dispositions)

- **T4-C-1 - committed evidence artifacts carry run-1 red while the table claims green** (evidence integrity, top nit). `run-test-gate.txt` EXIT 1 (1 suite red on rewrite-map staleness, 1306/1307), `round-facts.txt` EXIT 2 (verifier-broken spawn), `rewrite-map.txt` EXIT 1 (stale) - all committed at `1c08356` and never re-captured after the regen fixpoints. The honest-state section discloses run-1 red in prose; the evidence table rows nevertheless assert "full suite green" / "in sync" as artifact content. Audit re-ran all three green at HEAD verbatim (`audit-evidence/audit-*.txt`). Suggested repair: verbatim re-capture of the three legs post-fixpoint (the Disclosed Re-Capture channel exists for exactly this) OR amend the three table rows to name the red capture and cite the green re-run.
- **T4-C-2 - ref-assets committed against the task book's never-commit label** (process conformance). `next-round.md:8` marks `.scratch/grill-t23/ref-assets/` never-commit; `58ba705` committed logo.png/logo-dark.png/waitlist-banner.png and they remain tracked. They are NOT pipeline inputs (`build-brand-assets.py` reads `D:\NDM\generated-image.png`). No ledger record or report disclosure covers the deviation; the sweep's "never-commit set intact" claim is only true of the regex class. Suggested repair: untrack the three files (`but` removal + worktree bytes preserved) OR a ledger record amending the label - either way disclose.
- **T4-C-3 - consent sweep omits defer-0066** (same class as T3-C-2, which this very round backfilled). The chartered set names 0060/0064/0065/**0066**/0067/0068; the report's sweep skips 0066 - the harness-hardening row whose instances this round's own capture scripts touch (the C1 codePoint fix at `capture-battery.cjs:100` resolves registered instance 4, and T3-C-3's 'use strict' restore was itself labeled a defer-0066 instance - yet the registry row still lists all four instances open, no last_check_in). Suggested repair: dated amend-in-place backfill naming defer-0066 + registry note marking the C1 instance resolved-in-t23-harness.
- **T4-C-4 - C-7 lookback tick 2/2 not registered; split numeral wrong** (convention formality). Spec sections 1/8 + the t22 audit's own forward-note ("tick two of two is owed at t23") require the registration; the report uses split form ("17 captures + 1 fixture") but no tick line, and the numeral mismatches the committed set (25 captures + 1 fixture + 1 phase-6 sheet). Suggested repair: add the lookback-registration sentence with the correct count.
- **T4-C-5 - no per-finding disposition table for T3-C-1/2/3** (spec section 8 requires fixed/deferred/converted/rejected per finding). All three were verified fixed in substance; the report mentions them only inside the commit map. Suggested repair: add the one-row-per-finding table (all three -> fixed).
- **T4-C-6 - ADR-0079 D6 same-commit letter broken at `8effaaf`** (convention). The README-touching commit carried no zh update; the mirror sync + baseline re-pin were combined in `ec44625` (rhythm executed as README-only -> sync+repin rather than README+sync -> repin). Drift pin was red at the intermediate commit and that red is not individually disclosed (the coverage-pin red is). End-state invariant holds; `d597c8a` earlier did the rhythm correctly. Suggested repair: disclose in honest-state, or amend D6 wording if the combined form is accepted.
- **T4-C-7 - `config.yml` lacks the inline carve-out disclosure comment** claimed for "each template" (3/4 templates carry it; ci.yml carries ADR-0057 context but no carve-out comment either).
- **T4-C-8 - gh repo edit/view claim unevidenced** - the external-side-effect assertion rests on a commit message; the round's own verbatim-argv evidence bar was not applied to it. Audit verified the metadata live (`gh repo view` shows description + 7 topics). Suggested repair: capture a verbatim `gh repo view --json ...` leg.
- **T4-C-9 - record hygiene cluster** (cosmetic): `51a66d3`'s message describes a report edit its diff does not contain; commit-map rows mix sha and change-id tokens and omit `battery_as_of_commit 6e12154`; three headroom figures coexist (40,592 in registry vs 40,089 in ADR-0082 vs 39,710 implied by canon - each internally consistent on different bases); h1-reread/liveness `$` lines are display-form but unmarked (inherited t22 convention - defer-0066-adjacent).

## 5. Judgment/ambiguous - adjudicated, no defect found

- **trend row `advisory_fired: false` despite two advisories printing this round**: the field marks the row where the ADR-0064 streak first crossed K (grill-t7 holds `true`; every later row is `false` by convention). Consistent - field name invites misreading; not a defect.
- **Headroom tri-source values** (40,592 / 40,089 / 39,710): each derivable on a stated basis (pre-change M / post-commit / current canon). Registered under T4-C-9 as hygiene, not error.
- **The `evidence/` directory is committed while `audit-evidence/` is never-commit**: convention holds - round evidence is the deliverable; second-party evidence regenerates.
- **`capture-battery.cjs`/`recapture-clean-tree.cjs` cross-round cloning**: already registered as defer-0066 instance 1; it grew this round but inside the registered ratchet, so tracked-not-new.

## 6. Process observations (reported, not ratified)

Disclosed by the round, verified by this audit:

- **Two never-commit sweep incidents** - `a2e5571` swept 5 clean-tree.txt captures via a basename-grep id list; `51a66d3` absorbed all 168 staged never-commit candidates via the GitButler `A`-pool. Both reverted (`837fca7`, `0e951ea`), worktree bytes restored, committed tree clean. The second is the more serious process failure: an id-filtered commit still swept the staged pool - the round's own lesson (explicit allowlist) is the correct fix. Disclosure was full and timely; this audit reports, does not ratify.
- **GitButler index desync** mid-round (committed files surfaced as staged deletions) - repaired by `git reset` index resync, disclosed, no commit/branch state touched.
- **Mid-round reds** (README measurement block splice-back, stale coverage pin, battery run-1) - disclosed per the intermediate-red rule; final state green.
- **Not previously disclosed, surfaced by this audit**: ref-assets tracking (T4-C-2), the `8effaaf` same-commit deviation (T4-C-6), and the stale-red evidence artifacts (T4-C-1 - half-disclosed: prose names the run-1 reds, the table does not).

## 7. Residuals / standing surface

- `defer-0068` stays open by design - ADR-0082 second_reviewer countersign owed at the next audit window; review_at 2026-12-15 tide. This audit constitutes first-party-independent review of the channel but the registered slot asks for the countersign record - recommend discharging defer-0068's check at the next round's T-0 with this report as the review evidence.
- Social-preview upload remains manual (no API exists) - `docs/assets/brand/social-preview.png` 1280x640 awaits the user's two clicks per `docs/assets/README.md`.
- Burn-rate advisory at 4 consecutive carve-out rounds - next round should prefer R3 surfaces; a further R2 touch remains legal but re-fires the warning.
- `bench-gate`/`ci-wiring`/`mr-probes`/`probes` remain capability-absent on this host (disclosed, unchanged).
