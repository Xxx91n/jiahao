# grill-t19 fix-disposition round report (2026-09-19)

Kind: fix (ADR-0078 D-A). Scope: the grill-t18 audit's A-1..A-8 findings
and R-1..R-7 rework, per the task book at
`.scratch/grill-t19/handoffs/next-round.md`. No README or bilingual work -
t20 opens only on this baseline (T-4 out of scope).

## Facts canon

<!-- round-facts:start -->
- suites: 73
- passed: 1262
- skipped: 0
- pack_bytes: 339573
- instrument_entries: 27
- rewrite_map_citations: 1566
- registry_entries: 57
- anchors_count: 17
- battery_as_of_commit: 0f895b2
- report_commit: null
- not_run: [bench-gate, ci-wiring, mr-probes, probes]
<!-- round-facts:end -->

The facts region is rendered deterministically from
`.scratch/grill-t19/round-facts.json` (ADR-0077 D-C); `report_commit`
stays null - the report cannot cite its own commit. Prose carries no
canon number (ADR-0077 D-E unconditional scan); every declaration cites
an evidence path instead.

## Retroactive-repair registrations (Disclosed Repair, D-002)

Each repair leads with its declaration triple (reason + when + who);
who = grill-t19 fix/dev sub-agent, when = 2026-09-19:

- **A-1a** - t18 ledger T-3 consent-sweep: the `defer-0060` named line
  restored. Declaration on the line records the omission and the spec
  pre-scoping reason (spec section-8 + ledger D-008 scoped the sweep set
  without it while task-book item 22 included it). The row stands:
  pending-evaluation external-event (CI 403 carrier), quarterly,
  review_at 2026-12-15. Evidence: `.scratch/grill-t18/decision-ledger.md`
  T-3 sweep; commit `xqq`.
- **A-1b** - t18 report Consent-sweep paragraph: the same restoration.
  Evidence: `.scratch/grill-t18/reports/2026-09-19-report.md`; commit
  `xqq`.
- **A-2** - t18 report: export-misreport by-design note added, incl. the
  SCHEMA_KEYS/PROSE_KEYS test-only export surface. Evidence: same file,
  Process section; commit `xqq`.
- **A-5** - t18 report: the green-throughout sentence rewritten into the
  true statement (ruy red at commit, re-sync disclosed on the wrx line);
  ruy/wrx/wsl added to the commit-chain paragraph. Evidence: same file;
  commit `xqq`.
- **A-3** - Disclosed Re-Capture (D-006): the first capture of
  `.scratch/grill-t18/evidence/check-ci-jobs-missing.txt` is declared
  VOID - the invocation layer's escape interpretation ate the
  backslash-n sequences into literal LFs. Re-captured through a
  Non-Interpolating Channel (spawnSync arg array,
  `.scratch/grill-t19/recapture-check-ci-jobs-missing.cjs`,
  parameterized `.scratch/grill-t18/evidence/no-such-ci.yml` arg) with
  the fresh verbatim output written to the same slot via fs - a new
  capture, never a byte patch. The corrupted bytes are absorbed as the
  negative fixture of the new `.txt` signature leg. Evidence: commit
  `utp`; the re-captured file itself.

## Machinery dispositions (R2, disclosed via governance_tooling_diff)

- **A-6** - `scripts/build-round-facts.js`: `main()` body wrapped in
  try/catch mapping any crash (corrupt JSON.parse, spliceRegion throw,
  execFileSync/collect failure, requireCapabilities throw) to
  `verifier broken` + exit 2 - a crash never masquerades as unsatisfied
  (ADR-0077 D-A). Evidence: `.scratch/grill-t19/evidence/` corrupt-fixture
  pin in `test/adr-0076-wiring.test.js` (grill-t19 describe); commit
  `lrn`.
- **A-8** - same file: the missing-factsFile early-exit replaced by the
  missing-input clause - the unsatisfied condition is recorded via the
  drift channel at the single boundary exit, the report phase is
  skipped, and the three-value contract grows no fourth class
  (ADR-0077 D-A.1, clause appended in commit `pmv`). Evidence:
  `.scratch/grill-t19/evidence/round-facts-missing.txt` (FAIL pair +
  EXIT 1, fixture report unspliced); commit `lrn`.
- **A-4** - `scripts/check-governance-inventory.js`: kind:fix rows are
  outside both advisory streak populations (skip-not-reset) - they never
  feed the doc net-additions streak even at net_additions>0 and never
  reset carveStreak; ADR-0078 D-A carries the sentence; checker + ADR +
  pins coupled per ADR-0035. Evidence: wiring pins in the grill-t19
  describe; commit `twz`.
- **A-7** - same checker: `governance_tooling_diff` presence implies a
  non-empty `files` list for both kinds; 'no carve-out used' records as
  `carve_out_used:0` with the field omitted, never an empty files list.
  Evidence: wiring negatives in the grill-t19 describe; commit `txu`.

## Anti-rot quota discharge (D-004, registered)

The fix bundle's FIRST R2 machinery commit is `lrn` - the
build-round-facts crash-wrapper + missing-input fix (A-6/A-8), the
smell-ticket class the quota exists for. The discharge is the commit
ordering plus this registration line and the ledger Registration line,
not a claim.

## Zero-drift streak fact (D-003, registered)

Under the new skip-not-reset semantics the trend-anchor reading is
advisory_fired=1 = expected - zero advisory drift; the standing
'fired vs expected' drift warning resolved, and the advisory now reads
consecutive documentation rounds only. Evidence:
`.scratch/grill-t19/evidence/governance-inventory.txt`.

## Consent sweep

Per-row named lines (Consent Sweep convention) - every standing row
touched keeps its named line in `.scratch/grill-t19/decision-ledger.md`
T-3: defer-0060 (restoration registered, row stands), defer-0063
(discharged at t18), defer-0053 (frozen pending corpus), defer-0055
(sunset pointer, organic=0), defer-0057 (closed), defer-0058 (net
registry increment zero - fix rows exempt), sunset counter (1/6,
mid-quarter), O-E backlog (informational), never-commit patch set
(verbatim).

## Process and disclosure

- Commit chain on grill-t19-docs: `nwp` closeout-base (task-book
  acceptance: CONTEXT terms + ADR clauses + spec pre-scoping), `qzp`
  setup absorption (t18 audit report + handoff, disclosed), `xqq` t18
  Disclosed Repairs + cascade regen, `pmv` ADR-0077 D-A.1 clause, `utp`
  A-3 Disclosed Re-Capture + .txt leg, `krp` stale published_tip pin
  re-pinned disclosed, `lrn` machinery fix (first R2 - quota), `rsr`
  machinery pins, `twz` ADR-0078 + checker guard (coupled), `txu` gtd
  escape channel (coupled), then the closeout + evidence commit(s).
- Intermediate-commit greenness: one red mid-round state is disclosed
  here - the adr-0074 published_tip pin was stale for the span between
  the C2 cascade regen and its re-pin commit `krp` (the suite ran red on
  that pin only). Everything else stayed green through the round.
- Old-side discovery refs for the rewrite-map were restored disclosed:
  the `gb-local/grill-t11-docs` + `gb-local/grill-t12-docs` remote-
  tracking refs had been pruned; they were re-pointed at the retained
  old tips ab2e322/2c93a30 (objects present), keeping the committed map
  provenance form.
- `report_commit` stays null (self-citation is impossible); the
  zero-product-diff claim is honest (no R1 runtime file touched).
- Governance tooling diff on the t19 trend row lists exactly the two R2
  machinery source files hand-edited this round
  (`scripts/build-round-facts.js`, `scripts/check-governance-inventory.js`);
  `carve_out_used` omitted - no carve-out invoked.

## Battery

The grill-t18 section-6 battery re-ran verbatim parameterized
`--round grill-t19`, captured into `.scratch/grill-t19/evidence/`:

- `run-test-gate.txt` - suite-count gate
- `gate-all.txt` - all registered gates
- `check-ci-jobs.txt` - keyed exit on the live registry (deferral valid)
- `check-ci-jobs-missing.txt` - verifier-broken on a missing input
  (re-captured through the Non-Interpolating Channel)
- `check-deferred.txt` - deferred-registry gate
- `governance-inventory.txt` - inventory check + advisories
- `anchors.txt` - witnessed digest anchor check
- `rewrite-map.txt` - translation-point sync check
- `round-facts.txt` - facts canon + report region in sync
- `instrument.txt` - instrument gate
- `pack-smoke.txt` - pack smoke check
- `quoted-stale.txt` - the quoted-stale fixture MUST fail --check
- `inventory-shape.txt` - grill-t19 wiring pins
- `prose-fixtures.txt` - prose fixtures leg
- `h1-reread.txt` - committed .scratch doc-hygiene byte scan
- `liveness.txt` - pack, extract, install help, init dry-run, MCP
  initialize
- `round-facts-missing.txt` - the A-8 missing-input leg (FAIL pair +
  EXIT 1), captured via `capture-missing-facts.cjs`
