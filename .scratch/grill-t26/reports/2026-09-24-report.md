# grill-t26 round report - anchor semantics (claim-point pinning + terminal seal boundary)

- Round: grill-t26 (fix round; the treadmill-retirement round)
- Base: `bde0570b` (origin/main at round start) - landed stack on `grill-t26` lane
- Spec: `.scratch/grill-t26/spec-t26-disposition.md`; ledger: `.scratch/grill-t26/decision-ledger.md` (D-001..D-005)
- Task book: `.scratch/grill-t26/handoffs/next-round.md` (committed on the `grill-t26-docs` lane, `9674439f`)

## Facts

<!-- round-facts:start -->
- suites: 81
- passed: 1359
- skipped: 0
- pack_bytes: 350309
- instrument_entries: 27
- rewrite_map_citations: 2785
- registry_entries: 65
- anchors_count: 18
- battery_as_of_commit: 9fcf682e
- report_commit: null
- not_run: [bench-gate, ci-wiring, mr-probes, probes]
<!-- round-facts:end -->

## What landed

- **Shared checker** (D-005): `scripts/evidence-freshness.js` — single implementation of the two-layer semantics: non-anchoring classification, claim-commit detection, claim-point floor evaluation, stale-vs-invalid split, seal resolution (declared/expectedAnchor/inFlightClean/freeze), tag co-naming (`co-named`/`absent`/`drift`), unregistered-claim warning, `evaluateRound` driver. Carries the Metz fallback clause in its header (a round needing genuinely different walk semantics declares the deviation in its ledger and inlines locally — never piles conditionals into the shared checker).
- **Taxonomy registration** (D-003/D-005): `docs/governance/surface-taxonomy.json` gains the `freshness` block — closed `claim_surfaces` enum (`reports/`, `handoffs/`, with `handoffs/next-round.md` excepted as bookkeeping), the non-anchoring class registry, and the `rounds` registry ({id, base} triples for grill-t24/t25/t26).
- **Fixture suite** (D-005 hedge): `test/freshness-checker.test.js` — 7 legs on a synthetic repo: exempt classes never anchor; claim-point floor; stale detection; bookkeeping exemption; seal resolution + freeze; in-flight rejection; unregistered claim-like warning.
- **Suite migration** (D-005): `test/adr-0083-wiring.test.js` + `test/adr-0084-wiring.test.js` no longer re-roll the walk — both load the shared checker and pin the new semantics against real history: `D-A freshness under ADR-0085: claim-point conformance + tNN terminal seal at 8e177d24`. Provenance-header and coverage legs carried over unchanged.
- **Legacy back-registration** (D-004): `.scratch/grill-t24/SEAL` + `.scratch/grill-t25/SEAL` — both declare `seal: 8e177d2417eada69ff36b600aae74aa94c24ee34` with `recorded_at: 2026-09-24` (back-registration honesty: the writing date, not a back-date). Both suites verify the declared sha equals the mechanically derived last-substantive commit and that post-seal edits turn red.
- **Normative carrier** (T-3): `docs/adr/0085-anchor-semantics-claim-point-seal-boundary.md` — D-A two-layer semantics (floor-anchor at claim points / seal-anchor at the terminal declaration / stale-as-metadata / point-in-time position), D-B the SEAL carrier, D-C tag co-naming + `drift`/`absent` states + same-round lag bound + no-backfill, D-D machinery topology + retired-object registration, D-E landing tail. ADR-0083 D-A and ADR-0084 D-C gain pointer lines; `CONTEXT.md` gains **Claim Point** and **Seal Boundary** glossary terms.
- **Sweep**: `defer-0070` check-in names the three quarantined suite legs for t27 triage; t26 trend row (`kind: fix`, `adr_added: [0085]`, cumulative coverage declaration per the t21 C-1 rule); cumulative pin bumps (`85 records` index literals, `--expected-suites` bump (value lives in `.github/workflows/ci.yml` and the captures), `published_tip` re-pin `fc390d5`→`bde0570b`, named-not-positional trend-row lookup).
- **Evidence**: `.scratch/grill-t26/evidence/` — 12 legs, all `EXIT 0` at `9be511b0`.
- **Acceptance battery**: `.scratch/grill-t26/capture-battery.cjs` — re-runnable verbatim captures with durable `captured-at-head` headers (`git log -1 --invert-grep --grep='^GitButler Workspace Commit'` — the t25 ephemeral-sha lesson applied).

## Claim-point provenance (D-003)

This commit is the round's claim commit. Floor for this claim = `9be511b0` (last hard-anchoring commit strictly before it — the cumulative pin sweep). The committed evidence wave names `9be511b0` in every `captured-at-head` header ≥ floor → conformant. The wave-1 evidence commit `9fcf682e` is non-anchoring (evidence class) and does not raise the floor.

## Landing tail (D-E)

After this claim commit: regen commits (mechanism outputs only, non-anchoring) → terminal wave (all captures re-run at the post-regen tip, headers ≥ this commit) → `.scratch/grill-t26/SEAL` declaring this commit's sha as the round's last substantive act → nothing anchoring after the seal.

## Acceptance (验收标准 mapping)

- **编译通过**: pure-JS repo, no build step — `node --check` over the new/changed scripts + the full jest run is the compile-class evidence (`evidence/run-test-gate.txt` tail line `[test] OK` carries the verbatim suite/test counts; the canon numbers live in `round-facts.json`).
- **打包通过**: `npm pack` + `scripts/check-pack-smoke.js` (`evidence/liveness.txt` tarball section + `evidence/pack-smoke.txt`, EXIT 0).
- **启动并测活软件进程**: extracted-tarball `install.js --help` / `init --dry-run` and the `jiahao-mcp` JSON-RPC `initialize` probe returning `{"result":{"protocolVersion":"2024-11-05",...}}` (`evidence/liveness.txt`, EXIT 0).
- **每个平台 test 闭环**: this host is Windows — the full jest gate + `run-gates.js` + the checker's own fixture suite close the loop here (`evidence/run-test-gate.txt`, `evidence/gate-all.txt`; `test/freshness-checker.test.js` all legs pass). The public-clone tier degrades per the ADR-0084 contract where a capability is absent (`not_run` channel).

## Disclosures

- **Direct-push exception uses**: none — all commits landed on the local `grill-t26` lane via `but`; push/merge/tag remain user actions. The `adjudicated/grill-t26` tag is human-side, lag-bounded to this round (ADR-0085 D-C.4).
- **Declared-vs-tag on legacy seals**: `adjudicated/grill-t25` names `bde0570b` (the landed tip) while the back-registered `SEAL` declares `8e177d24` — the checker records the explicit `drift` state. This is the pre-contract tag pushed under the t25 convention (tag = tip endorsement); under ADR-0085 it stands as the recorded `drift` exemplar, not a defect to hide. t24's tag is `absent` → declaration-only degrade, also recorded.
- **Transitional red windows** (mid-round, resolved before this claim point):
  - `build-adr-index --check` red while the index lacked ADR-0085 (before the C3 regen).
  - Coverage legs red between the t26 trend row landing and the cumulative-declaration fix (the latest row must cover the committed diff since the earliest in-use coverage base `c526de3` — the t25 machinery set is redeclared).
  - `84 records` / `expected-suites 79` / last-row pins red between registry edits and the C7 pin sweep.
  - rewrite-map staleness between the prose commit and its post-commit regen (citation bindings depend on the commit graph).
  - First battery run: `tar` leg red — GNU tar parsed the drive-letter path as `<host>:<path>` ("Cannot connect to C: resolve failed"); fixed by relative-path extraction inside the temp dir (`27b7569a`).
  - Fixture-suite first run: `git commit -qm base` parsed `base` as a pathspec; fixed to `-m base` (prior session, recorded in the summary).
- **defer-0070 quarantine**: the three CI-red suite legs (`adr-0069-wiring` tag-resolution, `sentinel-ownership` env-sensitive assertion, `adr-0079-wiring` public-history pin) all pass on this host — the failures are CI-environmental, tracked to t27. defer-0070 stays pending-evaluation, owner named, review 2026-10-15, closes on the first green origin/main run.
- **Capability degrades**: `ci-wiring`, `bench-gate`, `probes`, `mr-probes` are `UNVERIFIABLE` on this host (no `ci-mode`) — recorded in `not_run`; they do not merge into fail (ADR-0040).
- **Engine provenance**: no online-research engine calls this round (the `atomcode-research` preference noted; nothing required fresh lookup — the ADR's structural lineage cites established analogues: TUF role separation, transparency checkpoints, RFC 6962 SCT-before-inclusion, SOC 2 point-in-time-vs-period).
- **Edit-channel note**: the repo's committed-artifact convention (`fs.writeFileSync` / file-edit tools, never escape-interpreting shells) was followed for all committed files; a mid-round JSON reformat regression (1-space vs 2-space indent) was caught by `git diff --stat` and re-emitted at the original indent before committing.
- **facts canon**: `report_commit` stays null by design (the report cannot cite its own commit); `battery_as_of_commit` pins the wave anchor — post-report regen commits do not re-splice (the report is anchoring).

## Evidence index

`.scratch/grill-t26/evidence/` — `captured-at-head: 9be511b0` on all 12 legs:

| leg | command | verdict |
|---|---|---|
| run-test-gate | `node scripts/run-test-gate.js --expected-suites <ci-pinned>` | EXIT 0 — every suite/test passes; verbatim counts in the file's tail line |
| gate-all | `node scripts/run-gates.js` | EXIT 0 — 4 unverifiable (ci-mode absent), 0 failed |
| governance-inventory | `--coverage-base bde0570b` | EXIT 0 — registry OK, coverage leg green |
| check-deferred | `node scripts/check-deferred.js` | EXIT 0 |
| adr-index | `node scripts/build-adr-index.js --check` | EXIT 0 — index in sync |
| rewrite-map | `--check` | EXIT 0 — map in sync |
| rewrite-map-published | `--published-only` | EXIT 0 — clone-verifiable subset holds, ancestry verified vs origin/main |
| anchors | `--check` | EXIT 0 — digests in sync |
| pack-smoke | `node scripts/check-pack-smoke.js` | EXIT 0 |
| liveness | pack/extract/install/mcp probe | EXIT 0 — tarball extracts, installer prints usage, MCP initialize returns result |
| clean-tree | `git status --porcelain` | EXIT 0 — verbatim capture (zz-pool leftovers shown honestly) |
| freshness-eval | `evaluateRound` over t24/t25/t26 | EXIT 0 — t24/t25 seal `8e177d24`, t26 claims conform, seals conditional until T-4 terminal step |

## Next-round seeds (t27 scope stub)

- Three quarantined CI-red suites to triage: `adr-0069-wiring` (tag-resolution exit-128), `sentinel-ownership` (environment-sensitive assertion), `adr-0079-wiring` (public-history SHA pin). Degrade-or-fix decision per leg.
- `defer-0070` closes on the first green workflow run on origin/main (record the run id).
- First live measurement of the convergence cost of the claim-point + seal semantics (waves-per-round under the new floor rule vs the 20+ t25 treadmill).
- Human-side: push `adjudicated/grill-t26` annotated tag naming the SEAL-declared sha; `adjudicated/grill-t25` drift resolution is a human adjudication, not agent scope.
