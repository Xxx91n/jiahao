# grill-t24 Declared-vs-Actual Drift round report (2026-09-23)

Task book: `.scratch/grill-t24/handoffs/next-round.md`. Round = ADR-0083
(four drift clauses) + carriers + closeout. Branch: `grill-t24-docs`
(GitButler). Base: `c526de3`. Not pushed.

## Completion definition

- T-0: t23 artifacts absorbed; rewrite-map input state repaired (old-side
  ref restored); fixpoint regen to `--check` green; clean-tree verify.
- T-1: ADR-0083 authored - D-A evidence freshness / D-B never-commit
  single-sourcing / D-C allowlist + post-commit inspection / D-D suite
  count sync / D-E audit-window inheritance.
- T-2: carriers - `docs/governance/never-commit.json` (nc-001..009),
  AGENTS.md clause bullets, `test/adr-0083-wiring.test.js` (new suite),
  ci.yml parity bump as declared carve-out, legacy literal re-pins,
  defer-0069, t24 trend row, README + zh-CN sync.
- T-3: verbatim battery under the new provenance convention (the
  `captured-at-head` header's first live firing), consent sweep, facts
  canon, audit handoff, per-commit boundary verification - this document.

## Facts canon

<!-- round-facts:start -->
- suites: 78
- passed: 1325
- skipped: 0
- pack_bytes: 340472
- instrument_entries: 27
- rewrite_map_citations: 2344
- registry_entries: 63
- anchors_count: 18
- battery_as_of_commit: c972b43
- report_commit: null
- not_run: [bench-gate, ci-wiring, mr-probes, probes]
<!-- round-facts:end -->

## Commit map (base c526de3 -> tip)

| sha | scope |
| --- | --- |
| e23579c | round seed: ledger D-001..D-004, spec, task book, GOAL, pre-landed glossary term |
| 30a72c0 | T-0 absorb t23 repair-verification (PASS, nine dispositions artifact-verified) + next-round handoff |
| 3501127 | T-0 fixpoint: rewrite-map regen after `gb-local/pre-purge-line` restore (old side re-anchored at retained tip); round-facts collected at absorb state |
| c972b43 | T-1/T-2: ADR-0083 + never-commit registry + AGENTS bullets + wiring suite + ci.yml carve-out + defer-0069 + trend row + README/zh-CN sync + cumulative tally re-pins + anchors regen |
| f8fcb45 | zh-CN translation baseline re-pin to the carriers commit (ADR-0072 D6) |
| authored-docs fixpoint | battery scripts (capture-battery + recapture-clean-tree, registry-driven classifier), this report, audit handoff, next-round handoff (this commit) |
| regen chain | mechanism-output fixpoints: rewrite-map / round-facts / anchors regen over the committed doc surface |
| evidence endpoints | acceptance battery captures under `captured-at-head` headers; clean-tree re-capture post-commit (idempotent endpoints, ADR-0083 D-A) |
| closing fixpoint | final rewrite-map regen over the committed evidence surface |

`battery_as_of_commit` names the durable tip the canon last collected
against; it is a run-record field stripped by `--check`, never a commit-map
row (t23 convention, unchanged).

## Carve-out disclosure (ADR-0076 D-B / grill-t24 D-002.5)

`carve_out_used: 1` on the `grill-t24-doc-round` trend row.
`governance_tooling_diff.files` (the single R2 touch, zero R1):

- `.github/workflows/ci.yml` - ADR-0057 D-C suite-parity bump forced by the
  new adr-0083 wiring suite; the obligation is now codified as ADR-0083 D-D.
  Inline disclosure comment updated in-file; the ADR-0083 wiring suite pins
  the ci.yml count equal to the live `glob(test/*.test.js)` count plus a
  lower bound and a known-file hit (the equal-wrong-values guard).

`mechanism_output_diff`: `bench/research/out/g6-publish-replay.json`
(faithful regen inside gate:all, same as t17..t23).

## Declared-vs-Actual drift surfaced this round (dogfood)

The round's own clause class fired twice before the policy even landed:

- **rewrite-map input drift**: the committed map declared old-side refs
  (`gb-local/grill-t11-docs`, `gb-local/grill-t12-docs`) that t23's ref
  pruning had deleted - `--check` red at baseline. Repair: a new pointer
  `gb-local/pre-purge-line` re-anchored to the retained old tip (all fifteen
  rewritten commits still reachable); map regenerated and committed. The
  input state, not the generator, had drifted.
- **registry seed vs tracked reality**: the first draft of nc-009 claimed
  `private/` as never-commit while `private/phrases.json` is a deliberately
  tracked fixture (committed since the ADR-0013 era); the rule was narrowed
  to `host-config-backup/` and `private/` stays under `.gitignore`
  jurisdiction. Five grandfathered tracked-tree instances (t7/t12/t13/t15
  audit captures committed before the convention existed) are enumerated
  in the wiring suite as a closed LEGACY set - disclosed, not purged
  (history rewrite is forbidden).

## Consent sweep

Chartered surfaces touched this round, named:

- `defer-0069` - **registered** (the ADR-0083 D-E audit-window check lines'
  first live firing), pending-evaluation at the quarterly tide.
- `defer-0068` - stays pending-evaluation by design (the t23 audit's
  countersign is recorded; the row rides to the review tide).
- `defer-0066` - instances 1-3 stay open; no disposition change this round.
- `defer-0067` - terminal since the ADR-0082 amendment (discharged).
- Never-commit set - **single-sourced this round**: the ad-hoc regex classes
  became `docs/governance/never-commit.json` (nc-001..009); capture scripts
  and the wiring suite consume the registry, not local labels. The
  `never-commit-sweep.txt` leg enumerates every rule and classifies every
  untracked path by rule id - the all-list run the task book demands.
- Sunset counter - unchanged.
- `zero_product_diff: true` - held: zero R1 files in the round diff
  (verified by the coverage leg, base c526de3).

## Evidence set

`.scratch/grill-t24/evidence/` - `captured-at-head` header + verbatim
`$ argv` + `EXIT n` + output (the ADR-0083 D-A provenance shape, first
live use):

| leg | verdict |
| --- | --- |
| `gate-all.txt` | all gates exit 0 (capability-absent legs disclosed via the facts canon `not_run`) |
| `compile-yaml.txt` | ci.yml parses (js-yaml; top-level keys + jobs enumerated) |
| `compile-node-check.txt` | `node --check` over the round's new scripts + wiring suite |
| `run-test-gate.txt` | full suite green at the declared count (see facts canon) |
| `check-ci-jobs.txt` / `check-ci-jobs-missing.txt` | real parse OK; missing file -> keyed exit 2 |
| `check-deferred.txt` | registry entries OK (count in the captured output) |
| `governance-inventory.txt` | inventory OK (advisories printed, disclosed) |
| `coverage.txt` | base c526de3: every R2 file in the committed diff declared by the latest row |
| `coverage-badref.txt` | unresolvable ref -> keyed FAIL exit 1 (no uncaught crash) |
| `anchors.txt` | governance digests in sync |
| `rewrite-map.txt` | map in sync |
| `round-facts.txt` | canon + report region in sync |
| `instrument.txt` | instrument state in sync |
| `pack-smoke.txt` | tarball under the amended cap (bytes in the capture + facts canon) |
| `adr-0083-wiring.txt` | the new suite verbatim |
| `never-commit-sweep.txt` | registry enumeration + every untracked path classified by rule id |
| `quoted-stale.txt` + `quoted-stale.fixture.md` | unconditional scan rejects a bare canon value + schema-key assign (exit 1) |
| `reclass-shape.txt`, `inventory-shape.txt`, `prose-fixtures.txt` | shape legs green |
| `h1-reread.txt` | independent byte scan over committed `.scratch/*.md|.txt` (artifact `$` line is display-form) |
| `liveness.txt` | pack -> extract -> `install --help` -> `init -y --dry-run` -> MCP `initialize` handshake (artifact `$` lines are display-form - the capture script renders the argv label) |
| `clean-tree.txt` | post-commit porcelain: tracked-diff 0, untracked = registry never-commit class or self |

Evidence set count (C-7 split form): the committed set is enumerated
artifact-by-artifact in `never-commit-sweep.txt` / `clean-tree.txt` rather
than asserted from memory; the suite floor (adr-0083 wiring) requires the
leg set to exist on disk, and the ordering pin requires every committed
capture to name a sha at-or-after the freshness anchor.

## Audit-window inheritance (ADR-0083 D-E)

The next audit window MUST include, verbatim as scope lines:

1. **evidence-freshness ordering check** - each committed capture's
   `captured-at-head` against the last anchoring commit (non-anchoring =
   evidence-only commits and faithful mechanism-output regenerations);
   implemented mechanically in `test/adr-0083-wiring.test.js`.
2. **never-commit label coverage** - registry rules vs committed tree and
   untracked set; `never-commit-sweep.txt` is the rerunnable artifact.
3. **commit file-list conformance** - every round commit's
   `git show --name-only` vs its declared allowlist; per-commit receipts
   live in the audit trail.

The first live firing is registered as `defer-0069` (pending-evaluation,
quarterly tide).

## Honest-state disclosures

- **Mid-round red, disclosed**: the adr-0083 wiring evidence-floor pin was
  red between the carriers commit and the first battery run (the floor is
  the anti-vacuous guard - it cannot go green before captures exist); the
  round-facts artifact stayed at its T-0 collect until the closeout fixpoint
  (its own `--check` leg read stale mid-round); the zh-CN baseline drift pin
  was red at the carriers commit and went green at the re-pin commit
  (ADR-0072 D6 two-commit rhythm, precedented).
- **Capability-absent legs, disclosed not silently skipped**: the corpus/
  CI-machinery legs listed in the facts canon `not_run` field fail closed
  on this host by design.
- **Facts-canonical ordering**: the acceptance battery's facts leg is
  check-only; collect/write/splice runs in the closeout orchestration so
  the canon is not rewritten mid-capture (ADR-0083 D-A split).
- **No push**: branch `grill-t24-docs` is local-only per the task book.

## Known blockers

- `defer-0068` rides to the quarterly tide (the countersign slot discharges
  at the review window, not this round).
- `defer-0066` instances 1-3 remain open (harness consolidation is a future
  round's work).
- Burn-rate advisory context: consecutive carve-out rounds - prefer R3
  surfaces next (advisory, not a block).

## Lessons learned

1. `captured-at-head` headers turn evidence files into doc citations: a
   rewrite-map regen MUST trail the evidence commit, which is why ADR-0083
   D-A defines the non-anchoring commit class - without it the ordering
   invariant cannot converge.
2. The never-commit registry's first fidelity test caught two drifts in
   its own seed: a `private/` rule claiming a path class that legitimately
   tracks a fixture, and five historical violations grandfathered as a
   closed LEGACY enumeration instead of being silently absorbed.
3. GitButler index desync persists: `git reset -q HEAD` before any
   porcelain-based leg; `but commit` rides explicit path-id allowlists;
   `git show --name-only` verifies the boundary after every commit.
4. The ci.yml parity pin moved from a literal-per-round to the glob-equal
   + lower-bound + known-file-hit form in the new wiring suite - the
   equal-wrong-values failure mode is now guarded structurally.

## References

- ADR-0083 `docs/adr/0083-declared-vs-actual-drift-clauses.md`
- Registry `docs/governance/never-commit.json`
- Trend row `grill-t24-doc-round` in `docs/governance/trend-inventory.json`
- Deferred entry `defer-0069` in `docs/deferred-registry.json`
- Wiring `test/adr-0083-wiring.test.js`
- Evidence `.scratch/grill-t24/evidence/`
- Audit handoff `.scratch/grill-t24/handoffs/2026-09-23-audit-handoff.md`

