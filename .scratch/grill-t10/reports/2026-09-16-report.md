# grill-t10 round report — hook-side conviction lane, shadow-mode ship

Round: grill-t10 | Date: 2026-09-16 | Branch: `grill-t10-docs` (GitButler)
Authority: `.scratch/grill-t10/handoffs/next-round.md`, `decision-ledger.md` (D-001..D-007), `spec-judge-seam-lane.md`, `GOAL.md`.

## Scope verdict

**Shipped: the conviction lane in shadow mode, with the frozen promotion gate.** The adjudicated CAPA pairer is now a shipped runtime artifact wired into Stop/SubagentStop; its findings are append-only telemetry records that never block and never enter the severity matrix. Enforce mode is NOT enabled this round — the registered stop point (D-007).

Commits on `grill-t10-docs` (base `a2f4faa`):
- `sqv` docs: judge-seam lane round spec + task book + CONTEXT terms
- `uvy` docs(grill-t10): ADR-0070 (T-1 doc round)
- `sww` feat(grill-t10): ship the conviction lane in shadow mode (T-2+T-3)
- `onz` fix(grill-t10): F-A corrigendum — re-render v3 md detail sections (T-4)
- `yxv` docs(grill-t10): round report (this file); `ron` audit report + handoff; audit F-B fix + addendum commits (post-report)

## What was built (per decision)

- **D-002 hook-side conviction lane.** `src/pairer-lane.js` runs on Stop/SubagentStop stdin: `transcript_path` → `src/transcript-adapter.js` → `pairItem` (the adjudicated `src/capa-pairer.js`) → a record appended to the evidence chain under `source: pairer-instrument` / `gate_id: pairer-lane`. flagged → `suspicious:true + severity:high + shadow:true`; consistent/undetermined → telemetry (`observed`), never a flag, never a block. `_idem` dedups re-fired hooks over identical (session, transcript-content) pairs.
- **D-003 frozen promotion gate.** Two-stage contract frozen in ADR-0070 D-B: ≥200 real Stop events spanning ≥1 full usage period; all flagged items owner-reviewed FP=0 (disagreement counts as FP); undetermined rate ≤90%; lane p99 latency ≤1 s. `scripts/pairer-lane-telemetry.js` computes G1/G3/G4 from the chain and keeps G2 an explicit human review. `.jiahao-conviction-off` is the channel-level kill switch surviving post-enforce; `.jiahao-conviction-enforce` is the registered promotion marker that drops the shadow bit.
- **D-004 product shape.** Single source: `bench/research/capa-pairer.js` moved to `src/capa-pairer.js` byte-identical (sha256 `9ff2d0ada931…`, 10,697 B — no dual copy). The adapter is a channel component, never the instrument (contract tests, not adjudication pins). `transcript-file` joins the closed capability enum (`JIAHAO_TRANSCRIPT_FILE` existence predicate); per-host reachability lives in `test/fixtures/host-contracts.json` (`present`: claude-code; `unverifiable`: codex/copilot/qoder; `absent`: instruction-tier hosts) and is asserted by `check-host-contracts.js`.
- **D-005 claims.** The three registered descriptive-existence sentences landed verbatim in `README.md`, `bench/research/out/claim-template.md`, and `bench/research/out/devin-oot-v3-report.md`; the per-mention binding registry gained the registered exemption for sentence 3; the reproduction invitation extends to the lane telemetry commands.
- **D-006 F-A carry-over.** F-A1: `scripts/corrigendum-v3.js` re-rendered the v3 md detail sections (confusion/FP/categorical/session-batch/honest-ratio/side-set) from the burned `report.json` in a dedicated corrigendum commit — `report.json` sha256 `fd6a0d42…` and the v3 manifest sha256 `57d44b89…` unchanged. F-A3 forward rule registered: `check-corpus-classes.js` validates `harness_ref` when a frozen manifest declares it (durable ref only; bare workspace sha is a remark; frozen manifests never rewritten). F-A4 errata rode the corrigendum commit (worker v2 self-reference fix; Sessions:20 main-set parenthetical). F-A2 recorded in ADR-0070: 备案，无需行动.
- **D-007 stop point honored.** No enforce flip; `.jiahao-conviction-enforce` is a registered marker, not an enabled state. The judge seam stayed untouched.

## Acceptance criteria (verbatim: 「编译通过、打包通过、启动并测活软件进程；每个平台都要有 test 闭环，避免只引入却没做到。」)

| Criterion | Evidence |
|---|---|
| 编译通过 | `npx jest` → **67 suites, 1064 tests, all pass** (incl. new `test/adr-0070-transcript-adapter.test.js` 9 tests, `test/adr-0070-pairer-lane.test.js` 10 tests, `test/adr-0070-wiring.test.js` 31 tests) |
| 打包通过 | `npm run gate:all` → **exit 0, 33 entries**; `[pack-smoke] OK: budget 302936 < 340000 bytes` — tarball carries `src/capa-pairer.js`, `src/transcript-adapter.js`, `src/pairer-lane.js` |
| 启动并测活 | `node scripts/install.js --help` → usage text, exit 0. End-to-end process test: `node hooks/jiahao-verdict-gate.js` fed a constructed transcript → shadow flag recorded, decision `allow` with advisory `Conviction lane (shadow): pairer flagged exit-report — claim 2 vs evidence 0`; `jiahao resolve --verdict pass --reason <r> --reviewer <r>` → `human_verdict recorded` (override path live); `node scripts/pairer-lane-telemetry.js` computes the four gate inputs |
| test 闭环 per platform | `test/adr-0070-*.test.js` (adapter contract / lane semantics / wiring), `test/adr-0069-*.test.js` repointed to `src/`, `host-contracts.test.js` covers the 11-adapter reachability field, `hooks.test.js` regression suite unchanged-pass |

## Rerunnable evidence

```
node scripts/check-pairer-regression.js
→ [pairer-regression] OK: 140 items replayed through src/capa-pairer.js
  (pinned 9ff2d0ad), all rows match the burned v3 report (36 flagged, 0 undetermined)

node bench/research/devin-oot.js --snapshot-dir devin-corpus-v3 --replay
→ [devin-oot-v3-replay] OK: stored artifact re-derives cleanly
  (falsification-passed, lie 36/36, FP 0/84, CI lower 0.902606)

node bench/research/capa-probes.js
→ probe record: ALL CATEGORIES MATCH

node scripts/corrigendum-v3.js --check
→ [corrigendum-v3] unchanged: detail sections already match report.json

node scripts/check-host-contracts.js
→ [host-contracts] OK - 17 contracts, 11 lifecycle entries (transcript_file tri-state asserted per host)

node scripts/check-deferred.js
→ [deferred] OK - 45 deferred entries

node scripts/build-governance-anchors.js --check / check-governance-inventory.js
→ OK - 9 artifacts in sync / OK: 33 entries, live source_adr, unique commands

git diff --check → clean; `but status` → working tree clean at report time
```

## Honest notes (no overclaims)

- `gate:all` reports 4 UNVERIFIABLE gates locally (`ci-mode` capability absent — `ci-wiring`, `bench-gate`, `probes`, `mr-probes`); expected outside CI, not a failure.
- The governance-trend check emits its non-blocking advisory (ADR added this round; net_additions registered as defer-0050).
- The pack-smoke cap was amended this round: 300,000 → 340,000 bytes via **ADR-0071** (`docs/adr/0071-tarball-cap-trend-anchor-amendment-t10-conviction-lane-surface.md`; defer-0051 review_at 2026-12-15). The 302,936-byte tarball passes the amended cap; under the prior 300k cap it would have failed. (Disclosure added post-audit, F-B1.)
- The lane's `undetermined`/`absent` states are telemetry: nothing is hidden from denominators; a host without `transcript_path` produces NO record and NO escalation.
- Shadow-mode behavior asserted end-to-end: flagged pairer output recorded + surfaced as advisory + `allow`; a lane-only shadow chain still counts as no-evidence for the verifier profile (block) — the instrument never masquerades as agent evidence.
- No PR opened, nothing pushed (per workflow); no enforcement-efficacy claim — v3 remains a decision-table outcome on a seeded corpus.

## Remaining work (handoff)

- Bake window: accumulate real Stop events until G1 (≥200 events, ≥1 full usage period) is measurable on the telemetry surface; G2 stays an owner review.
- Promotion flip to enforce is a separately registered action after the bake window — out of this round.
- `npm publish` still deferred (per round scope); the deferred-registry tide (45 entries) continues its cadence.
