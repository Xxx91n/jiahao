z# Handoff \u2014 grill-t6 T-1 implementation round complete (2026-09-14)

## Where things stand

Branch: grill-t6-t1-round @ yro (implementation szy + audit repair yro), stacked on grill-t6-doc-round @ qxs
(main untouched, merged upstream). All ADR-0064 T-1 tasks (D-002..D-006)
landed and verified. Full evidence table: .scratch/grill-t6/reports/
2026-09-14-report.md. Working tree was clean at commit time.

## What the next round owns

1. Confirmatory round (ADR-0064 D-B): pick the settled rung-2 candidate
   (survivors.json + rung-2 table in out/attribution-report.md), adopt the
   JS port (bench/research/sklearn-port.js + g6-manifest.json) into src/,
   and run the T-6 confirmatory bench against the pinned corpus. Success is
   the MDE stop-loss gate (D-A), NOT the absolute targets.
2. Devin truth collection: scripts/collect-devin-corpus.js drop \u2192 validate
   \u2192 snapshot; unlock after rung-1 settlement only; never feed rung stats.
3. defer-0040 disposition at the next documentation-round evaluation
   (zero-product-diff event; review 2026-12-14).
4. Watch the trigger-mask finding: +0.1093 delta says the public corpus
   carries label-correlated lexical artifacts. Any confirmatory claim must
   discuss this honestly in the report.

## Hard rules that still bind

- All writes via but; no raw git mutations; no push/PR/tag/release without
  explicit ask. Independent branch per session.
- Thresholds move only via same-commit ADR (ADR-0027 D2 coupling).
- d_MDE stays frozen at 0.084663 for this round (re-freezing = new round).
- Public-tier tests skip only through test/helpers/skip.js.
- advisory surfaces (trend anchor) warn once and never block.

## Suggested skills for the next round

- $implement for the adoption line; tdd at the port seam; code-review
  before commit; $handoff to close.
- atomcode-research only if new external literature is needed (m2cgen /
  deployment-side validation already surveyed).

## Environment

- python 3.11 + sklearn 1.9.1 + m2cgen 0.10.0 installed (research lane).
- Pinned corpus clone: %TEMP%\jiahao-polygraph-994bdeb\u2026 (data/ has
  items.jsonl + labels.jsonl). If temp is cleaned, re-clone at the pinned
  ref.

Audit-repair deltas (yro): corpus-bound tests are tier-gated (fullT/skipTest);
freeze surface includes private_corpus; g6 manifest carries idf + token_spec;
tier-(b) is diagnostic-advisory (ruling in ADR-0064); D-B carve-out recorded;
attribution report carries the D-001 P/R/increment/adversarial/usability
sections; devin disjointness is item-level; net_additions is recomputed;
.scratch is excluded from jest scanning (package.json testPathIgnorePatterns).
