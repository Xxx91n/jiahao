# grill-t11 round report - readiness verdict: usable + testable (measured)

Round: grill-t11 | Date: 2026-09-16 | Branch: `grill-t11-docs` (GitButler)
Authority: `.scratch/grill-t11/handoffs/next-round.md`, `decision-ledger.md` (D-001..D-005), `spec-readiness-and-dispositions.md`, `GOAL.md`.

## Scope verdict

**The product is formally usable and testable, and the declaration is measured, not asserted.** The R1 documentation round (commit `vyr`) landed ADR-0072 plus every governance disposition; the R2 action round (commit `xvw`) executed the pre-registered clean-environment re-measurement on the live install channel - all steps green - and landed the layered, evidence-source-tagged README declaration.

Commits on `grill-t11-docs` (base `c861084`):
- `wnx` docs(grill-t11): readiness round spec + task book + CONTEXT terms (prior session)
- `vyr` docs(grill-t11): ADR-0072 + all R1 governance artifacts (T-1 doc round)
- `xvw` feat(grill-t11): R2 re-measurement record + README declaration (R2 action round)

## What landed (per decision)

- **D-002 readiness verdict + measure-then-declare.** ADR-0072 D-A registers the layered verdict (usable+testable; NOT detector-effectiveness, NOT gate-passed). D-B pre-registered the b2-method re-measurement plus the downgrade trigger; D-G registered the declaration block dormant. R2 executed it: clean HOME/work + cold npm cache, `npx -y github:Xxx91n/jiahao init --profile verifier -y` exit 0, profile flag written; mock Stop no-evidence -> exit 2 block, evidence appended -> exit 0 allow; mock Stop carrying `transcript_path` (exit-report contradiction) -> exactly one `source:pairer-instrument` shadow record (flagged, shadow:true) AND the shadow-only home still blocks on no-evidence. Record: `.scratch/grill-t11/readiness/b2-remeasurement-2026-09-16.md`; chain event: instrument-state seq 19. The README "## Readiness status (ADR-0072)" block is now live, each line tagged [installed-artifact measured] / [documented].
- **D-003 bake protocol + tier-2 amendment.** Owner-dogfood on claude-code is the registered bake subject; hook registration on the owner host is gated behind a separate owner confirmation (host-config change) - STOP POINT, not done this round. ADR-0070 G2 amended via registered discipline: flagged items get independent second-line review before the flip is evaluated; pre-set FP/TP adjudication criteria registered; disagreement resolves toward FP. Synthetic bake traffic forbidden; single-operator traffic supports plumbing-health claims only.
- **D-004 four critique dispositions.** P-1: ERRATA E-6 discloses second_reviewer=Xxx91n as delegated signature under standing authorization (verbatim on-record from seq 10; critique anchored on seq 13; registered wording "delegated second-line review, ID-level + delegation-level independence") + ADR-0047 appended note; frozen history untouched. P-2: forward rule (scope+expiry mandatory from next authorization-bearing event) + pre-staged template `docs/governance/delegation-renewal-template.md`. P-4: ADR-0067 appended appendix registering INDETERMINATE de facto claim treatment (equal mechanical binding; reserved label stays reserved). P-5: Re-Execution Prior term verified present in CONTEXT.md.
- **D-005 R1/R2 boundary + capability labels.** `measured-present` word slot added to the transcript_file enum (validator + _doc definition); no contract carries it - reserved until the first real claude-code lane event. The ADR-0070 claim sentence-1 parenthetical amended in all three claim homes to `present` (documented to deliver, not live-measured); the ADR body keeps its registered text and carries the appended amendment note.

## Verification evidence (rerunnable)

| Check | Command | Result |
|---|---|---|
| Full test suite | `npx jest --silent` | 68/68 suites, 1088/1088 tests, exit 0 |
| Public tier | `JIAHAO_TEST_TIER=public npx jest --silent` | 68/68 suites, 1081 passed, 7 skipped (corpus-bound, with reasons) |
| Gate suite | `npm run gate:all` | exit 0, 33 entries, 4 unverifiable (ci-mode gates; honest three-state on a non-CI host) |
| Pack + smoke | `node scripts/check-pack-smoke.js` (inside gate:all) | jiahao-0.0.1.tgz 305,755 B / 109 files extracted, packaged CLI dry-run matched, budget 305755 < 340000 |
| Instrument chain | `node scripts/instrument.js --check` | exit 0, state authoritative, seq 18+19 appended |
| Deferred registry | `node scripts/check-deferred.js` | OK, 46 entries (defer-0052 landed) |
| Governance anchors | `node scripts/build-governance-anchors.js --check` | OK, 10 artifacts in sync (decision-ledger-t11.md + ERRATA digest) |
| Inventory | `node scripts/check-governance-inventory.js` | OK, 33 entries; trend-anchor advisory fired (streak 5, advisory only) |
| Host contracts | `node scripts/check-host-contracts.js` | OK, 17 contracts, measured-present slot registered-and-unused |
| ADR index | `node scripts/build-adr-index.js --check` | in sync, 72 records |
| R2 measurement | see `.scratch/grill-t11/readiness/b2-remeasurement-2026-09-16.md` | all steps PASS; downgrade trigger registered but not engaged |

## Stale-count repair (rides along)

README claimed "994 tests across 63 suites" while disk held 67 suites / ~1070+ tests (t9/t10 additions never synced the counts). Now: 68 suites / 1088 tests / 7 public-tier skips - all machine-verified above.

## Open items (next round inputs, not defects)

- Owner confirmation needed before claude-code hook registration (modifies host config) - the bake window opens on that act.
- First real lane-bearing Stop event flips claude-code transcript_file to `measured-present`.
- Promotion review stays gated on the bake window (0 real events today).
- Transcript reachability on codex/copilot/qoder remains `unverifiable` (registered non-blocking agenda).


## Addendum 2026-09-17 - owner-host registration executed

Owner granted authority in-session ("授权给你权限，把所有内容都必须做完"). The
last R2 residual - claude-code hook registration - is now DONE:

- Stable install: `npm pack` tarball of grill-t11-docs HEAD extracted to
  `C:\Users\Administrator\.jiahao\pkg` + `npm i --omit=dev` (the earlier
  `npm i -g github:` attempt landed a broken _cacache symlink - removed).
- settings.json merged (backup: `.scratch/grill-t11/host-config-backup/
  settings-pre-jiahao-2026-09-17.json` + sibling `.bak`): 6 jiahao entries
  across SessionStart/SubagentStart/UserPromptSubmit/Stop/SubagentStop/
  SessionEnd, commands pinned to `D:/nodejs/node.exe` + pkg path.
- Flags: `.jiahao-profile=verifier` + `.jiahao-active=full` planted in BOTH
  `~` and `~/.claude` (hook-env config-dir resolution is not documented -
  dual-plant covers either branch; dormant copy is inert).
- Liveness (scratch CLAUDE_CONFIG_DIR, real cmd invocation, zero real-chain
  writes): activate injects verifier ruleset; mode-tracker/subagent/sweep
  exit 0; Stop no-evidence exit 2 block; Stop+transcript wrote a shadow lane
  record (flagged, exit-report, claim 2 vs evidence 0, session reg-check-flag)
  and still blocked - identical semantics to the R2 measurement.
- instrument-state seq 20 records the change under the verbatim grant with
  bounded scope/expiry per the P-2 forward rule.

Pre-existing host state discovered during registration: `.jiahao-active`
(2026-08-22) and `.jiahao-evidence` (189 gate records, 0 pairer-instrument)
already existed - dev-harness writes, not bake traffic. Bake corpus counting
must filter to `gate_id=pairer-lane` records written after 2026-09-17.

Still open (unchanged): first REAL lane-bearing Stop event flips
claude-code to measured-present; promotion review waits for the bake window.
