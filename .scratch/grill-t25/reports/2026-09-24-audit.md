# grill-t25 audit — second-party verification of the fix round (public-clone verifiability + merged sweep)

- Auditor: second-party audit agent (audit window — report only, no fixes)
- Object under audit: `grill-t25-docs` + `grill-t25-fix` stack, 45 commits over base `fc390d5` (public tip at round start; `main`/`origin/main` still there — nothing pushed, consistent with D-004)
- Report under audit: `D:\Aworker\jiahao\.scratch\grill-t25\reports\2026-09-24-report.md`; handoff `D:\Aworker\jiahao\.scratch\grill-t25\handoffs\2026-09-24-round-handoff.md`; evidence `D:\Aworker\jiahao\.scratch\grill-t25\evidence\`
- Method: report self-description NOT trusted — every load-bearing claim re-verified against the repo (fresh `--no-local` clone acceptance re-run in full; all gates re-run on the maintainer host; file/commit/registry spot-checks) plus the $code-review dual axis (Standards + Spec sub-agents, diff `fc390d5..grill-t25-fix`).

## Verdict

**SUBSTANCE VERIFIED — RECORD-LAYER REPAIRS REQUIRED (bounded rework, then this audit re-runs its checklist).**

Every load-bearing claim is true: the clone-degradability mechanism works on a real fresh clone, the full acceptance battery is green at the tip on both the public-clone shape and the maintainer host, all six ADR-0084 clauses landed, all sweep registrations are real, and no negative-union item was violated. The defects found are confined to the record layer: one pinned trend-field mislabel, two mangled test titles, three terminal evidence files that record red at the tip, and an emptied facts-canon region. None changes the verified state; all violate this repo's own bookkeeping precision rules (ADR-0076/0077/0078).

## Hard acceptance — auditor re-run (not the report's captures)

Maintainer host (`D:\Aworker\jiahao` at workspace tip = `grill-t25-fix` tree; `git diff grill-t25-fix HEAD` empty):

| Leg | Command | Reported | Auditor re-run |
| --- | --- | --- | --- |
| compile | `node --check` ×7 touched scripts/suites | EXIT 0 | EXIT 0 ×7 |
| yaml | `js-yaml` parse `.github/workflows/ci.yml` | OK | OK (jobs: gate-all, test, summary) |
| rewrite-map | `node scripts/build-rewrite-map.js --check` | EXIT 0 | EXIT 0 (2710 citations, in sync) |
| published-only | `node scripts/build-rewrite-map.js --published-only` | EXIT 0 | EXIT 0 (2710 covered, ancestry vs origin/main) |
| gate:all | `node scripts/run-gates.js` | EXIT 0 | EXIT 0 (36 entries, 4 unverifiable = ci-mode set; [208] rewrite-map PASS, [209] rewrite-map-published PASS, [218] secret-scan PASS incl. `commit-messages 549`) |
| deferred | `node scripts/check-deferred.js` | OK 65 | OK 65 (53 live) |
| ci-jobs | `node scripts/check-ci-jobs.js` | EXIT 1 expected | EXIT 1 (defer-0004 unmet — disclosed carried residual) |
| anchors | `build-governance-anchors.js --check` | OK 18 | OK 18 |
| instrument | `instrument.js --check` | OK | OK (conditional cert, disclosed warning) |
| pack | `node scripts/check-pack-smoke.js` | OK | OK (345326 B, 113 files — byte-identical size to evidence) |
| jest corpus | `npx jest --silent` | 79/79, 1345 | **EXIT 0 — 79/79 suites, 1345 passed, 0 skipped** (maintainer corpus tier) |
| round-facts | `build-round-facts.js --round grill-t25 --check --report <report>` | disclosed drift | **EXIT 1 — `round-facts.json is stale` + `report facts region is stale`** (see F-C/F-D) |

Fresh public-clone acceptance (auditor's own clone, `git clone --no-local .` → `C:\...\tmp.4HJysm907p\repo`, clone HEAD `75af717` = workspace commit = stack tree; `refs/remotes/gb-local/` count = 0 — the public-clone shape):

| Leg | Auditor result |
| --- | --- |
| `npm ci` | exit 0 (268 packages) |
| `npx jest --silent` | **exit 0 — 79/79 suites, 1338 passed + 7 skipped = 1345** (corpus-bound tier skips, ADR-0056/0057) |
| `node scripts/run-gates.js` | **exit 0 — 36 entries, 10 UNVERIFIABLE** (ci-mode + bench-corpus + `old-side-refs`; `rewrite-map` degrades to UNVERIFIABLE, `rewrite-map-published` PASS), 0 failures |
| `--check` | **exit 2** with the registered two-line UNVERIFIABLE channel (`gate=rewrite-map, requires=old-side-refs`) |
| `--published-only` | **exit 0** (2710 citations, ancestry verified vs clone's origin/main) |
| `check-pack-smoke` | exit 0 |
| liveness | `npm pack` → extract → `install.js --help` / `init -y --dry-run` all exit 0 |

Verdict on hard acceptance: **PASS, independently reproduced** — the fourth mandatory audit line (ADR-0084 D-B) is green at the tip from a real clone.

## Claim → evidence → conclusion (report "What landed" / disclosures)

| # | Claim | Auditor evidence | Conclusion |
| --- | --- | --- | --- |
| 1 | Absent `gb-local/*` → `--check`/`--verify`/write exit 2 UNVERIFIABLE via `capability.exitUnverifiable`, zero direct exit-2 | `src/shared/capability.js` (enum + `old-side-refs` probe via `for-each-ref` minus gitbutler internals + HINTS + `exitUnverifiable`); `build-rewrite-map.js` routes both probe-negative and post-discovery edges through it; observed live: clone `--check` exit 2, maintainer exit 0 | CONFIRMED |
| 2 | `--published-only` asserts clone-verifiable subset; registered as `rewrite-map-published` gate | `verifyPublishedOnly()` (coverage + class enum + counts + published ancestry); `docs/gates.json` order-209 entry, `requires:["repo-tree"]`, command + source_adr pinned; exit 0 both hosts | CONFIRMED |
| 3 | ADR-0084 six clauses; ADR-0083 gains only pointer line | 0084 file carries D-A..D-G incl. `old-side-refs` verbatim + fourth audit line; 0083 diff = exactly one added line (line 54); wiring test pins D-E still 3 bullets | CONFIRMED |
| 4 | gates.json: `old-side-refs` added to rewrite-map requires + order-209 entry | diff verified verbatim | CONFIRMED |
| 5 | defer-0070 (40-run CI-red, owner + 2026-10-15) + defer-0071 (monitoring defer) | registry entries read: owner "repo maintainer (Xxx91n)", review_at 2026-10-15, discharged note, 40-run window 09-11..09-23 named; defer-0071 review 2026-12-15; structural diff: 63→65 entries, **only the two added, zero modified** (the 2242-line diff is a 1→2-space reformat — churn, not content) | CONFIRMED (+reformat churn noted) |
| 6 | `bbf5259` = first Re-Execution Prior directional event; n=19 untouched | CONTEXT.md term region carries the registered directional event, `n=19` prose intact | CONFIRMED |
| 7 | 10 countersign-queue ADRs: ID-level-only labels + return condition + 2026-12-15 | grep: exactly the 10 ADRs (0064-0070,0072-0074) carry `ID-level-only, awaiting entity-level` + `return condition:` + `return-by: 2026-12-15` in Status lines; wiring test pins all 10 | CONFIRMED |
| 8 | secret-scan 4th surface = commit messages; GitHub-enumeration gap noted honestly | `commitMessages()` enumerates `git log` reachable history (maintainer-only gb-local messages deliberately excluded — correct scoping); comment carries the honest note; live gate output `commit-messages 549` | CONFIRMED |
| 9 | `test/adr-0084-wiring.test.js` new suite incl. real git-init clone-sim exit-2 leg | file exists (229 lines, 16 tests); clone-sim leg does real `git init` + asserts status 2 + two-line channel | CONFIRMED |
| 10 | suite count 78→79 across ci.yml + README + era literals + zh mirror same-commit | ci.yml `--expected-suites 79`; README "79 suites / 1345 tests / 84 records"; README-zh-CN.md synced + `translation-baseline` re-pinned to the count-lines commit (ADR-0079 D6 second step, own commit `c405d47`); era literals re-pointed (83→84 record counts, ≥79 suite floor, positional→named row lookup) | CONFIRMED |
| 11 | ADR-0040 capability pin → charter map | adr-0040-wiring now anchors each capability to its chartering ADR (0084 charters `old-side-refs`), preserving append-only | CONFIRMED |
| 12 | Ordering invariant generalized to `.scratch/grill-tNN/evidence/` ∀NN | both wiring suites use `/^\.scratch\/grill-t\d+\/evidence\//`; disclosed in report | CONFIRMED (see Treadmill note §Findings) |
| 13 | ft-0004 re-pointed to grown enum | falsify.js twin now asserts `length===6` honest / `===7` liar; enum is 6 | CONFIRMED |
| 14 | trend row kind:fix + `governance_tooling_diff` names every R2 hand-edit | row exists; gtd files = the 6 machinery paths; g6 replay correctly in `mechanism_output_diff`; **but `zero_product_diff:false` is a mislabel — see F-A** | PARTIAL — F-A |
| 15 | clone-acceptance leg: fresh clone passes install/jest/gate-all/pack-smoke | auditor re-ran the full loop on an independent clone — all green | CONFIRMED |
| 16 | intermediate reds disclosed in commit trailers | trailers exist on earlier waves (335333b, e12e30a, 6ace652 "clone-sim transitional red disclosed"); **but the terminal-wave commits carrying the final red evidence files (24b1a5e/6c52b16/614e7a4/f18f66f/9ee8bbe) have empty bodies** — see F-C/P-2 | PARTIAL |
| 17 | "committed round-facts.txt legs carry the canon-as_of drift shape (same shape as the t24 tip)" | committed files at tip show **EXIT 2 verifier-broken** (jest spawn artifact), not a drift-verdict; t24's own-tip file at `fc390d5` was **EXIT 0 green** — the "same shape" comparison is accurate only for the t24 file *as re-captured this round*; live tip leg is genuinely EXIT-1 stale (canon-as_of drift — verified by auditor) | INACCURATE WORDING — F-C |
| 18 | "all legs green except the disclosed canon-as_of drift pair" (commit `6c52b16` subject) | the same commit lands `run-test-gate.txt` EXIT 1 (ordering leg red) — the red is not a canon-drift leg; subject overclaims | INACCURATE — P-2 |
| 19 | tool-sweep event disclosed + repaired (7 never-commit files swept then uncommitted) | current tree: `git ls-tree` shows zero `audit-evidence/` files added this round (the 4 in-tree files are pre-existing t12/t13/t15 history); index clean (0 staged); 186 never-commit leftovers sit untracked — the disclosed residual stands | CONFIRMED + standing hazard |
| 20 | human-authority package drafted, agent stops | `human-authority-package.md` carries the three drafted decisions (renew-or-expire 3 options, countersign forced binary, ratchet-brake → charter layer); nothing executed | CONFIRMED |
| 21 | report facts canon per precedent | report Facts sentinel region is **empty placeholder at tip** — it was populated at `22b1ed2` and emptied by `a8b18b4`'s rewrite; `--check` leg red at tip for region-stale in addition to canon-stale | DEVIATION — F-D |

## D-xxx compliance (decision-ledger)

| D-id | Requirement | Evidence | Verdict |
| --- | --- | --- | --- |
| D-001 | coordinate + sweep both in scope | both landed | OK |
| D-002.1 | R1: recapture wave + tag + clone-degrade + 4th audit line; clone-verified acceptance | all landed; tag pending-human (declared) | OK (tag = declared pending) |
| D-002.2 | R2 sweep completeness | all five items landed | OK |
| D-002.3 | R3 drafted, agent never decides | package drafted; no execution | OK |
| D-002.4 | R2 follows R1 closely | same round, ordered | OK |
| D-002.5 | ratchet-brake criteria → charter layer | proposed in package, unadopted (human decides) | OK |
| D-003.1 | stop-bleed/capture isolated; mechanism before capture | `36d0fb5`/`aeed246`/`e202fd5` precede first capture `cde72b8`; no mechanism+capture commit mixing | OK |
| D-003.2 | 0084 carrier + 0083 pointer-only | verified verbatim | OK |
| D-003.3 | t25 wiring re-roll (BASE=fc390d5, EVD=t25) + monitoring deferred | suite carries both; defer-0071 registered | OK |
| D-003.4 | third-party-reproducible trio (command/output/environment) | clone-acceptance.txt has command+output+head; **environment leg absent** (no node/OS line) | MINOR GAP |
| D-003.5 | order (a)→(e) | landed; (d) tag pending human | OK |
| D-004.1/2/3 | user executes push/PR-merge/tag | nothing pushed/merged/tagged by agent; all on local stack | OK |
| D-004.4 | exception narrowness + per-round disclosure | ADR-0084 D-D; report discloses zero agent direct-pushes | OK |
| D-004.5 | tag timing declared | declared | OK |
| D-004 negatives | no mechanism+capture mixing; intermediate reds disclosed | isolation holds; disclosure exists but generic — terminal-red files unnamed (F-C) | PARTIAL |
| D-005.1 | sweep order a→c→d→b, each item shaped as spec'd | all four landed; order expressed inside one commit `46ee8ee` (spec didn't require separate commits) | OK |
| D-005.2 | R3 package = minimal approval set | three items, charter-layer for meta-rules | OK |
| D-005.3 | kind:fix + gtd; round 3 no diff | row kind:fix + gtd complete; **but `zero_product_diff` mislabeled** (F-A) | PARTIAL |
| Negatives union (spec §7) | none | none violated — verified item by item | OK |

## Findings

### Must-fix before round close (record-layer)

- **F-A `zero_product_diff:false` mislabel, pinned by the new test.** Registered semantics (ADR-0076 D-A, ADR-0078): "no R1-surface file touched", R1 = `scripts/install.js` static require-closure (`docs/governance/surface-taxonomy.json`, 10 files). The round touches zero R1 files — `src/shared/capability.js`/`falsify.js` ship in the tarball but are NOT in the R1 closure; the row's own `reason` says "All are R2 machinery hand-edits". Truthful value is `true` (t22/t23/t24 all `true`); `test/adr-0084-wiring.test.js:216` pins `toBe(false)`, locking the mislabel. This is the t17-mislabel class that ADR-0078 exists to correct — fix by in-place correction to `true` with the correction disclosed on the row itself, and flip the test pin. Conservative direction, still a mislabel.
- **F-B mangled test titles ×2** (collateral of the bulk literal bump): `test/adr-0059-wiring.test.js` `'ADR-84 records the ADR-0038 D1 amendment'` (was ADR-0059); `test/adr-0060-wiring.test.js` `'ADR-0039 D84 records no recompute'` (was D3). Assertions unchanged; titles now misname what they pin — repair the literals.
- **F-C terminal evidence files record RED at tip, undisclosed per-file.** Committed at tip: `run-test-gate.txt` EXIT 1 (ordering leg red — the stale `clone-acceptance.txt` header 9bd1afd at capture-head 24b1a5e, fixed by 614e7a4 but the leg never re-captured); `adr-0084-wiring.txt` EXIT 1 (same leg at a8b18b4); `round-facts.txt` EXIT 2 (jest-worker spawn artifact — the same artifact the clone-acceptance note names); the re-captured **t24** copies of run-test-gate/round-facts show the same reds. Tip state is green (proven by auditor re-runs), so these are honest stale-red records — but the report never names them as terminal artifacts, `6c52b16`'s subject overclaims "all legs green except the disclosed canon-as_of drift pair", and the report's "canon-as_of drift (same shape as the t24 tip)" describes neither the committed bytes (verifier-broken) nor the t24-tip comparison (t24's own-tip file was EXIT 0). Fix per convention: a closing re-capture wave at the final tip (landing-tail) and/or precise disclosure text naming each red file and its real failure class.
- **F-D report Facts canon region emptied at tip.** Populated at `22b1ed2`; `a8b18b4`'s disclosure rewrite reset it to the bare placeholder. The live `--check` is consequently red on *two* legs (canon-stale + region-stale); only the canon drift is disclosed. Either re-splice from the committed canon (region leg greens; canon staleness stays the disclosed residual) or declare the placeholder the intended terminal form with the disclosure text fixed.

### Nits / judgement calls (record, no bounce required)

- ADR-0084 D-G: "`deferred_entry` names the round's defer ids" (plural) vs the single-valued field carrying only `defer-0071`; `defer-0070` is unnamed in the row (named in D-G text + report). Align wording or field.
- D-003.4 trio: clone-acceptance lacks the environment leg (no node/OS capture) — add one line next capture.
- `check-secret-scan.js` header comment "the same three rules scan it" — `scanMessage` applies only `kind==='content'` rules (2 of 3; R3 is a path rule). Comment overclaims.
- `test/rewrite-map.test.js` `HAS_OLD_SIDE` re-implements the `old-side-refs` probe inline instead of `cap.probe('old-side-refs', …)` — a second driftable copy of the logic this round centralized.
- `verifyPublishedOnly`: `removed[]` silently skips malformed `r.old` where sibling sections flag non-hex40 shapes — asymmetric validation strictness.
- `adr-0084-wiring.test.js` clone-sim leg leaks its `mkdtempSync` dir (contrast `rewrite-map.test.js`'s `fs.rmSync`).
- `$ <placeholder>` argv lines in evidence files are unmarked display-form (B-2 convention asks verbatim-or-marked) — **standing looseness shared with t24**, not a t25 regression.
- **Treadmill consequence (design note for the next round):** the generalized ordering invariant makes EVERY prior round's evidence dir freshness-tracked against the moving tip — each future round's anchoring commit forces a re-capture of all earlier evidence dirs (this round already re-captured t24's set). Disclosed and satisfiable, but the cost is now structural.

## Process ledger (violations & disclosures — reported, not ratified)

- **P-1 (disclosed by fix window, verified):** the `clean-tree` capture commit's basename allowlist swept seven prior-round never-commit files into an evidence commit; repaired via `but uncommit <commit>:<file>`; disclosed in the report (ADR-0083 D-C event). Current tree confirms the repair. Residual hazard stands: 186 untracked never-commit leftovers remain sweep-eligible — future allowlists must match full paths.
- **P-2 (found by audit):** commit `6c52b16` subject "all legs green except the disclosed canon-as_of drift pair" is inaccurate for the content it committed (`run-test-gate.txt` EXIT 1 is an ordering-leg red, not a canon-drift leg). Commit-message accuracy lapse — the repo's own machinery exists to catch exactly this.
- **P-3 (found by audit):** the report's claim "mid-round jest reds were disclosed in commit trailers" — the terminal-wave commits carrying the final red evidence have empty bodies; disclosure exists but lives on earlier-wave commits and as generic report prose, not where the red artifacts land.
- **P-4:** `docs/deferred-registry.json` (+2242) and `docs/governance/trend-inventory.json` (+803) carry unrequested 1→2-space reformats riding the real changes — semantically verified identical (63→65 entries, only the two new), but the churn inflates review surface.
- **No undisclosed external effects:** nothing pushed/merged/tagged by the agent; `origin` untouched; no `adjudicated/grill-t25` tag exists yet (human action per D-004/ADR-0084 D-E); no Tavily/engine calls claimed or found.
- **but-commit discipline:** the disclosed sweep event is the only allowlist incident; post-commit `git show --name-only` verification cannot be retro-proven but the committed tree matches disclosures.

## Rework requirements for the fix window (then this audit's checklist re-runs)

1. `zero_product_diff` → `true` on the t25 trend row with an ADR-0078-style correction note on the row; flip the pin in `test/adr-0084-wiring.test.js`; also fix or annotate the `deferred_entry` naming (defer-0070) vs ADR-0084 D-G wording.
2. Unmangle the two test titles (`ADR-84`→`ADR-0059`, `D84`→`D3`).
3. Re-capture `run-test-gate.txt`, `adr-0084-wiring.txt`, `round-facts.txt` (both dirs) at the repair tip — landing-tail wave — or amend the report's disclosure to name each terminal-red file with its true failure class.
4. Decide the facts-region terminal form (re-splice vs declared placeholder) and make the disclosure sentence match the committed bytes.
5. Optional nits above as the window sees fit.
6. Commit `D:\Aworker\jiahao\.scratch\grill-t25\reports\2026-09-24-audit.md` (this file) — it is currently uncommitted; committing lands an anchoring commit, so sequence it inside the rework's own landing-tail re-capture.

**Rerun checklist (the audit's leg set):** `node --check` on touched files → `npx jest` full corpus → `node scripts/run-gates.js` → `--check` + `--published-only` both modes → `check-pack-smoke` → `check-deferred`/`check-ci-jobs`/`governance-anchors`/`instrument --check` → `build-round-facts --check --report` → fresh `git clone --no-local` → `npm ci` → jest → run-gates → `--check` exit 2 + `--published-only` exit 0 → pack/liveness — plus `git status` clean-tree sweep and the evidence-header ordering legs.

## Suggested skills for the next window

gitbutler (rework commits + landing-tail wave), code-review (re-verify the repair diff), handoff (round handoff once repairs pass this same audit checklist).
