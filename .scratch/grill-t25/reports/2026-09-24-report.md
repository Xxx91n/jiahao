# grill-t25 round report - public-clone verifiability + merged sweep

- Round: grill-t25 (V6 critique response; fix round)
- Base: `fc390d5` (public tip at round start) - landed stack on `grill-t25-fix`
- Spec: `.scratch/grill-t25/spec-t25-disposition.md`; ledger: `.scratch/grill-t25/decision-ledger.md`

## Facts

<!-- round-facts:start -->
- suites: 79
- passed: 1345
- skipped: 0
- pack_bytes: 345325
- instrument_entries: 27
- rewrite_map_citations: 2571
- registry_entries: 65
- anchors_count: 18
- battery_as_of_commit: 8814a68
- report_commit: null
- not_run: [bench-gate, ci-wiring, mr-probes, probes]
<!-- round-facts:end -->

## What landed

- Mechanism: `scripts/build-rewrite-map.js` treats absent `gb-local/*` old-side refs as a capability negative - `--check`/`--verify`/default-write exit 2 UNVERIFIABLE via the registered two-line channel (`src/shared/capability.js` `exitUnverifiable`, keeping the ADR-0041 zero-direct-exit-2 pin); `--published-only` asserts the clone-verifiable subset (citation coverage, class enum, count self-consistency, published-side ancestry) and is registered as the `rewrite-map-published` gate.
- Normative: `docs/adr/0084-*.md` carries the six clauses (clone-degradability contract naming `old-side-refs` verbatim; fourth mandatory audit line = public-clone jest + gate:all; landing-tail wave convention; stop-bleed exception narrowness; tag timing; first-disclosure-channel rule). ADR-0083 gains only the pointer line.
- Registry: `docs/gates.json` adds `old-side-refs` to the rewrite-map gate requires + the new order-209 entry; the capability enum + probe register the name.
- Sweep: `defer-0070` back-registers the 40-run CI-red history (owner + 2026-10-15 review); `defer-0071` registers the generalized-monitoring defer; `bbf5259` registers in CONTEXT.md as the first Re-Execution Prior directional event (n=19 untouched); all 10 countersign-queue ADRs carry ID-level-only labels with a return condition and 2026-12-15 return-by; `check-secret-scan.js` gains the commit-message enumeration surface (the GitHub-official enumeration lacks it).
- Wiring: `test/adr-0084-wiring.test.js` (new suite, incl. a real git-init clone-sim exit-2 leg); the ADR-0040 capability pin moves to a charter map; the ADR-0083 D-D suite-count sync obligation re-rolls ci.yml + the legacy literals for the new suite (verbatim numbers in `.scratch/grill-t25/evidence/run-test-gate.txt`); coverage legs re-anchored to the t25 base; cumulative tally bumps across the era literals (README records, registry seed, trend-row count, named-not-positional row lookup); ft-0004 re-pointed to the grown capability enum.

## Disclosures

- **Direct-push exception uses**: none executed by the agent - all commits landed on the local `grill-t25-fix` stack via `but`; push/merge/tag are user actions per D-004.
- **Tag timing**: the `adjudicated/<round>` annotated tag is pending the human push (ADR-0084 D-E declares the dependency; the ADR-0069 tag legs stay red on a public clone until it lands).
- **Intermediate red states**: mid-round jest reds were disclosed in commit trailers - ordering invariant between anchoring commits and capture waves; coverage before the t25 row; falsify ft-0004 inversion before re-point; rewrite-map mid-run staleness during battery writes; the first ADR-0084 suite draft`s unescaped paren leg.
- **Freshness vs canon**: the committed canon names `battery_as_of_commit` at collect time - the field is drift-exempt by design (the closing commit always moves HEAD past the pin). The report region and canon agree as of the report-landing commit; post-report regen commits do not re-splice (the report is anchoring).
- **Ordering generalization**: the D-A anchor classification exempted only the suite's own evidence dir; with two coexisting evidence dirs a cross-round evidence commit became anchoring for the other suite, making the invariant unsatisfiable (the last evidence commit always post-dates the other dir's headers). Both ordering legs now treat `.scratch/grill-tNN/evidence/` as non-anchoring for every NN - evidence commits are regen-class events, not content changes. Without the generalization the residual stays permanently red; with it the semantics stay faithful (the anchor still tracks real content commits).
- **Tool sweep event (ADR-0083 D-C)**: the `clean-tree` capture commit's id allowlist matched `clean-tree` filenames under prior rounds' `audit-evidence/` dirs - seven never-commit-class files (`grill-t19`..`grill-t23`) landed inside the evidence commit, turning it anchoring and breaking ordering until detected. Repaired by `but uncommit <commit>:<file>` (files returned to untracked state; commit rewrote to the intended two). The residual lesson: uncommitted leftovers matching the sweep pattern are offered as addable ids - allowlists must match the full path, not the basename.
- **Index/tree divergence**: `but uncommit` leaves restored files staged (`A` in the index); `scanDocTokens` enumerates `git ls-files` (index) so the maintainer map recorded a site the committed tree lacks - `--published-only` then failed on a real clone while `--check` stayed green locally. Fixed by unstaging (`git restore --staged`) + regen; the clone-acceptance leg now runs the full install/jest/gate-all loop on a fresh `--no-local` clone.
- **Clone acceptance**: `.scratch/grill-t25/evidence/clone-acceptance.txt` records the fresh public clone passing `npm install`, the full jest corpus, `run-gates.js` (exit 0; rewrite-map degrades to UNVERIFIABLE via `old-side-refs`, `rewrite-map-published` PASS), `--check` exit 2 / `--published-only` exit 0, and `check-pack-smoke`.
- **Engine provenance**: CI history read via `gh api` (authenticated as the repo owner); no Tavily engine calls this round.
- **Known residual**: `check-ci-jobs` fails until defer-0004`s conditions land (expected, carried); the committed `round-facts.txt` legs carry the canon-as_of drift shape (same shape as the t24 tip - the canon is as-of the wave anchor and the closing regen commits add the wave's own header sites); on the maintainer host the corpus-secret gates stay unverifiable-listed where the secret is absent (the `not_run` channel in facts).
- **Audit rework (post-audit bounded fix, `grill-t25-audit` findings)**: the audit re-ran the acceptance legs independently - all green - and returned four record-layer findings. Corrections: (F-A) the t25 trend row's `zero_product_diff` was mislabeled `false` beside an all-R2 reason - zero R1-surface files changed this round, so the field is corrected to `true` and the wiring pin now asserts the honest value (the pinned-wrong-label class ADR-0078 exists to prevent); (F-B) the record-count literal sweep clobbered two test titles (`ADR-0059` -> `ADR-84`, `ADR-0039 D3` -> `D84`) - restored to the originals; (F-C) three committed terminal evidence files record red legs captured mid-fixpoint (`run-test-gate` + `adr-0084-wiring` ordering legs caught a stale clone-acceptance header; `round-facts` the disclosed canon drift) - they are re-captured at the terminal tip and named here; (F-D) the facts region had reverted to the splice placeholder at the report-landing commit - re-spliced from the canon so region==canon again, leaving the single disclosed canon-as_of drift.
- **Commit-subject discrepancy (disclosed, not rewritten)**: `6c52b16`'s subject claims "all legs green except the disclosed canon-as_of drift pair" but the staged re-capture it committed also carried the two ordering-red legs named in F-C. The subject stays as-committed - rewording mid-stack rebases every descendant and orphans the shas the committed evidence cites; this bullet is the record correction.
- **Process notes**: trailer disclosure applied to the early waves only - the terminal commits carry empty bodies; the deferred-registry and trend-inventory absorbed an unrequested whitespace reformat (verified semantic no-op); `grill-t25-audit` (audit deliverable `cb03a1a`) was restacked atop the fix lane per the audit separation convention.
- **Ephemeral-citation defect (found by the clone acceptance during rework)**: a mid-rework capture batch wrote `captured-at-head` naming the GitButler Workspace Commit - a synthetic object that never reaches a public clone. The clone's ordering/provenance legs failed on exactly those headers (`git cat-file` could not resolve the sha). Fixed by re-capturing with the durable-tip resolution the battery scripts already use (`git log -1 --invert-grep --grep=^GitButler Workspace Commit`); every committed header now names a real branch commit. Lesson: under a multi-lane applied workspace, `git rev-parse HEAD` is NOT a durable citation - the workspace merge commit is ephemeral.

## Evidence

All acceptance legs are committed under `.scratch/grill-t25/evidence/` with `captured-at-head` provenance; the clone-sim leg records the public-clone shape (clone -> no `gb-local/*` -> `--check` exit 2 UNVERIFIABLE + `--published-only` exit 0). The t24 evidence set was re-captured at the post-normative tip per the landing-tail convention.

## Next-round seeds

- Human: push the annotated `adjudicated/grill-t25` tag naming the landed tip sha.
- defer-0070 closes on the first green CI run; defer-0071 review_at 2026-12-15.
- The ten countersign labels return at the 2026-12-15 audit window (entity-level verdict or formal downgrade - forced binary).
