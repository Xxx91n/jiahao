# grill-t13 T-3 re-audit (post-fix) — 2026-09-17

Verdict: **PASS**. Independence grade (ADR-0074 D-F, verbatim): **weak-independent
— different session, same host + same toolchain** (node v24.11.0, npm 11.19.1).
Scope unchanged: preregistered three objects + fix-round claims under test.

Fix commit under audit: `uqk` = 505d3db on `grill-t13-r1` (stacked on audit
commit `rwk` = 876b09d). Fix report:
`D:\Aworker\jiahao\.scratch\grill-t13\reports\2026-09-17-t3-fix-report.md`.

## Rerun battery (auditor-executed, this worktree)

| Check | Result |
|---|---|
| `node scripts/run-test-gate.js --expected-suites 71` | OK — 71/71 suites, 1148/1148 tests, 0 skipped |
| `node scripts/run-gates.js` | exit 0 — 35 entries, 4 expected-UNVERIFIABLE (ci-mode) |
| `node scripts/check-secret-scan.js` | OK — "3 rules, 0 hits - enum index/tree/union 851/859/859; oversized-skip 0" |
| `node scripts/build-rewrite-map.js --check` / `--verify` | OK / VERIFY OK — 1182 citations, 15 pairs |
| `node scripts/check-deferred.js` | OK — 52 entries (48 live, 4 closed/actioned) |
| `node scripts/instrument.js --check` | OK — conditional cert, seq 24 record_only_change pending_signoff |
| `node scripts/check-pack-smoke.js` | OK — 322314 < 340000; packaged CLI executed |

## F-1..F-9 disposition verification

| # | Fix claim | Auditor evidence | Conclusion |
|---|---|---|---|
| F-1 | union enumeration (ls-files ∪ ls-tree HEAD) + runtime-assembled fixtures + loud oversized skip + enumeration test | `trackedFiles()` unions index+tree, exports `enumCounts`; output prints `enum index/tree/union 851/859/859` — union now covers the test file (859 ≥ tree); committed test bytes carry only disjoint fragments (`'-----BEGIN ' + 'RSA…'`), planted runtime bytes still trip R1/R2; `enumeration covers the committed tree` test present; oversized files push to `skipped-oversized` list | **Verified** — the CI-red path is closed and the mechanism is test-pinned |
| F-2 | A4 corrected + corrections[] registered | reverify json A4: "1 ! + 12 = + 2 left-only + 4 right-only… substantive check carried by --verify"; `corrections[]` entry dated+ref'd; r2-report gains "## Corrections registered after T-3 audit" (append-only, 0 deletions in file diff) | **Verified** — correction registered, not silent. Auditor note: fixer's "12 =" is the correct count; my "13 =" in the T-3 report was a miscount, they registered it in their deviation section |
| F-3 | numstat-level leg recorded | evidence A3 now carries `1+1- / 1+57- / 21+0-` matching the frozen plan's named shapes | **Verified** |
| F-4 | defer-0051 reopened pending-evaluation, own legs retained | status=pending-evaluation; original unfreeze (2026-12-15 check-in + pinned-protocol re-measurement + second_reviewer countersign) intact; distinct content stays on the entry, not folded into 0058 | **Verified** |
| F-5 | 0054 post-action external-event trigger + detection_limit | unfreeze_if.type=external-event "rules >3 OR miss incident → gitleaks"; detection_limit "blind to unknown secret forms" on the entry; adr-0073-wiring asserts all three | **Verified** |
| F-6 | 0053 cost_note | cost_note present (dirty-data window described, T-3 F-6 ref) | **Verified** |
| F-7 | entropy deviation registered in 3 places (registry/scanner header/ADR bullet) + rationale (sha256 digests in .scratch reports would false-positive a generic entropy rule) | Rationale is sound and the substitution is registered rather than silent | **Verified** |
| F-8 | byte-exact pin restored | POINTER_HEAD two-line literal + `toBe(POINTER_HEAD + '\n\n' + scratch)` — the weakened substring form is gone | **Verified** |
| F-9 | strict ×2, anchors, gitbutler filter, same/null forms, counts | `'use strict'` in both new scripts; filter `indexOf('gitbutler')===-1` real (old_refs=[t11-docs,t12-docs] only); map gains `same[]` (278, unchanged sub-boundary commits) — `removed` stays [] vacuous; GOAL/README counts synced 1148/1182 | **Verified** |

## Process review of the fix round

- `.scratch` published records: ledger + r2-report diffs are pure appends (0
  deletion lines). Corrections were registered, not silently edited — exactly
  the required shape.
- instrument seq 24 `record_only_change` (pending_signoff) with before/after
  registered; governance copy + anchors regenerated and green.
- No scope creep observed in the fix diff; the intentionally-untracked
  `.scratch/grill-t13/audit-evidence/r2-round-diff.patch` remains untracked
  (audit review copy — must never be committed, it carries F-1 fixture bytes).
- jest count moved 1147→1148 (new enumeration test inside an existing suite;
  suite count stays 71, ci.yml untouched — consistent).

## Residuals (open, registered — not defects)

- defer-0051 stays live: 2026-12-15 pinned-protocol re-measurement +
  second_reviewer countersign pending.
- instrument seq 24 pending human sign-off (not the audit window's to give).
- Push remains owner-gated; D-005e's dependency is now eligible to present
  (disclosure/claim artifacts are audit-passed).
- defer-0053/0055/0057/0058 + O-E continue per their cadence/unfreeze.

T-3 narrow close-audit closes: re-verification chain, disclosure entries, and
the rewrite map all stand on evidence.
