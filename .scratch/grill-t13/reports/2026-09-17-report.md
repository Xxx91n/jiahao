# grill-t13 R1 doc round — 2026-09-17 report

窗口：修复/开发子 Agent（grill-t13 会话）。分支：`grill-t13-r1`（GitButler 栈，立于 `grill-t13-docs` 之上，基于 published tip 051744a）。本轮 = 常驻任务书 T-1（R1 文档轮），覆盖账本 D-001/D-002/D-003/D-005；D-004 属 T-2 未动。无 push（owner-only 权限）。

## 落地物（commit 9e716dd, +655/-17, 20 files）

- D:\Aworker\jiahao\docs\adr\0074-sanitized-history-publish-rewrite-map-reverification-preregistration-independence-grade.md — 新 ADR：D-A 发布事件记录（决策/授权/有界后果：PR-diff 失效、clone/fork 重克隆纪律、SHA 全变的有界范围、commit-message 元数据残留披露）；D-B 披露三载体+指针注记惯例；D-C rewrite-map 单一翻译点（三分类+完备性不变式+append-only+禁 refs/replace）；D-D tip-pinned claim 失效规则 + verified-at 注记式；D-E 复验计划预注册（A1–A5 断言 + 证据五字段 + npm v12 pin + 白名单 diff + downgrade 触发措辞）；D-F 独立核验触发条件 + 独立性等级声明 + push 拆分登记；D-G 本轮面（defer-0057、t13 账本入锚、wiring seeds、R1→R2 门）。
- D:\Aworker\jiahao\docs\governance\sanitized-history-runbook-2026-09-17.md — 清洗 runbook：事实 + 6 条可复跑验证证据 + before/after 对应表；不复述被涂字面量/被删内容。
- D:\Aworker\jiahao\docs\rewrite-map-generator-spec.md — `scripts/build-rewrite-map.js` 生成器 spec（输入/对齐算法/三分类/输出 schema/三种模式/护栏）。
- D:\Aworker\jiahao\README.md — Distribution boundary 披露行 + Install verified-at pending 注记位 + 计数行同步（74 ADR / 1129 tests / 70 suites）。
- D:\Aworker\jiahao\docs\adr\0073-*.md、D:\Aworker\jiahao\docs\governance\decision-ledger-t12.md — 各加一行登记措辞指针注记（ADR-0074 D-B 登记原文）。
- D:\Aworker\jiahao\docs\governance\decision-ledger-t13.md — .scratch 原件逐字节治理副本（sha256 f335fb05…）。
- D:\Aworker\jiahao\scripts\build-governance-anchors.js + t13 行；D:\Aworker\jiahao\docs\governance\anchors.json 重生成（12 artifacts）。
- D:\Aworker\jiahao\docs\governance\trend-inventory.json + grill-t13-doc-round 行（advisory_fired=false，回放正确）；D:\Aworker\jiahao\docs\deferred-registry.json + defer-0057（51 entries）。
- D:\Aworker\jiahao\test\adr-0074-wiring.test.js（17 断言/6 describe）；adr-0033/0069/0070/0072/0073 wiring 惯例性 stale-count 维修；ci.yml expected-suites 69→70。
- D:\Aworker\jiahao\src\instrument-state.json — record_only_change seq 22（schedule_gate，pending_signoff；signoff 依惯例留待评审轮）。
- bench/research/out/g6-publish-replay.json — gate 链机械刷新（g6-publish 门自产，非手改）。

## 验收证据（全部可复跑，逐条对应）

### 1. 编译/静态面 — n/a（纯文档轮，无 src 行为改动；Jest 套件即本仓 test 闭环）

```sh
npx jest
```
→ Tests: 1129 passed, 1129 total; 70 suites（基线 1112/69 → +17 tests +1 suite = 新 wiring 文件；零失败）。

### 2. 打包通过

```sh
npm run pack:smoke
```
→ `[pack-smoke] smoke OK: jiahao-0.0.1.tgz (313884 bytes, 109 files) extracted and its CLI dry-run plan matched`; budget 313884 < 340000。

### 3. 进程启动并测活（本轮可达形态：安装器 dry-run 实测；真实 install+活性复验为 T-2 已预注册项）

pack:smoke 提取 tarball 后对安装 CLI 跑真实 `init --dry-run` 计划比对（上条输出）。发布尖真实清洁环境 `npx github:` 复验属 R2（ADR-0074 D-E 已预注册断言/证据字段/downgrade 措辞）。

### 4. 每平台 test 闭环

本仓 CI 平台面 = ubuntu-latest 单平台（.github/workflows/ci.yml：gate:all + run-test-gate 两 job + success-only 聚合）。本机 Windows 主机已跑同等闭环：

```sh
node scripts/run-test-gate.js --expected-suites 70
```
→ `[test] OK: 70 suites, 1129 tests, 0 skipped (ADR-0057 D-C)` — 与 CI 行同参数同断言。

### 5. 门链全绿

```sh
npm run gate:all
```
→ exit 0（33 entries；4 门 ci-mode-only 标 UNVERIFIABLE —— 本机预期形态，非失败）；instrument-identity OK（authoritative, conditional cert 2026-12-11）；governance-inventory 33 entries OK；advisory 警告 = 回放正确的 trend-anchor 提示（streak 7≥K=2，never blocks）。

```sh
node scripts/check-host-contracts.js && npm run adr:gate && npm run deferred:gate
```
→ 17 contracts/11 lifecycle OK；README ADR index in sync；51 deferred entries OK。

```sh
node scripts/build-governance-anchors.js --check && node scripts/build-adr-index.js --check
```
→ 12 artifacts digests in sync；index in sync。

### 6. 白名单 diff 事实底座（复验计划的断言对象）

```sh
git diff 2c93a30 3454d13            # → 空输出（双侧对应树逐字节一致）
git diff 2c93a30..051744a --stat   # → 精确 3 文件：audit.md 2行改 / round-diff.patch 58 / reaudit-closed.md +21
git log origin/main --oneline -- .scratch/grill-t11/host-config-backup/ jiahao-0.0.1.tgz   # → 空（双空证据之一）
git ls-tree -r origin/main --name-only | grep -E 'host-config-backup|jiahao-0\.0\.1\.tgz'  # → 空（双空之二）
```

### 7. 字节完整性（windows_file_integrity_protocol）

全部 19 个改动文件：LF、UTF-8 合法、无 BOM（node TextDecoder fatal 校验通过；t13 治理副本 9262B 与 .scratch 原件 sha256 相同）。

## 偏差与边界

- `test/adr-0073-wiring.test.js:159` 的治理副本断言放宽为「登记头注行 + 原件逐字节」（原断言要求逐字节相等；任务书要求头注落在治理副本上、.scratch 零改动 —— 二者只能以此调和，已按本仓「登记措辞 verbatim + wiring 钉死」惯例把注记原文锁进断言）。
- instrument record_only_change seq 22 处于 pending_signoff（与近期惯例一致：record 当轮落地、signoff 待评审轮）。
- 未跑 R2 动作：rewrite-map 生成器未实现（只有 spec）、复验未执行（只预注册）、registry dispositions 未动 —— 全部是任务书排序要求。
- WORKFLOW.md 实体不存在（已全局搜证）；§4.2 版本控制语义取自 grill-adr0058 修复报告 §5 + 全局规则 GitButler 节，执行形态一致（but 专用分支、不 push、偏差已披露）。

## Code review（双轴评审已跑，发现项已修复并 amend 进提交）

两轴并行评审（Standards + Spec）覆盖 diff `grill-t13-docs...HEAD`（907 行）。

**Standards**：无硬性违规；发现项与处置——
- (已修) adr-0033 wiring 尾注把 defer-0057 误归 ADR-0073 → 拆分为 defer-0053..0056(ADR-0073) + defer-0057(ADR-0074)。
- (已修) adr-0069 wiring 计数断言旁的过时注释 `// ADR-0073 landed (grill-t12)` → 改标 ADR-0074。
- (已修) README verified-at 槽位用 <sha> 而 ADR-0074 D-D 登记式为 <full-sha> → README 对齐登记式，wiring 断言同步。
- (已修) 0074 wiring 测试标题称注记「at the head of ADR-0073」而实际位于 References 之后 → 标题改为 sits on / heads（两表面措辞各归其位）。
- (采纳) README 边界行指向尚未生成的 docs/rewrite-map.json → 加 (generated in the R2 action round) 限定。
- (记录不改) POINTER_NOTE 在 0073/0074 两个 wiring 测试各持一份字面量——符合本仓套件自含惯例，第三次出现再抽公共 const。
- (记录不改) runbook 引用被删 tarball 的 sha256（来自已发布 commit message 元数据，属验证面引用，非内容复述）。
- CONTEXT.md 四术语确认已随前会话 commit 5c1fe1f 入库（AGENTS.md 要求的 glossary 同步满足）。

**Spec**：七项 T-1 交付物全数在；约束全部守住（无 src 行为改动、无 CHANGELOG、无 refs/replace、无 T-2 越界、不 push）。发现项与处置——
- (已修) 前向规则要求「入 ADR-0074 后果段」：规则正文在 D-D，Consequences 段已补登记句（ledger D-001 item 4）。
- (已修) runbook 双空证据未用注册命令形态：补上 `git log --name-status -- <paths>`（空）与 `git log -S``<literal>``（空）注册形态，并记录 -S 对路径 token 会合法命中、探针须绑字面量的实测细微差。
- (已修) anchors 语义披露：带注治理副本不再与 .scratch 原件逐字节相等 —— ADR-0074 D-B 已登记该后果（anchors.json 仍钉副本字节，登记注记行为唯一许可偏差）。
- (已核) Spec 评审无法自验的事实（295 commits、tip 全 SHA、区域 SHA、哈希链）已由主会话逐条命令核实，见上方证据节。
- (记录) g6-publish-replay.json 机械刷新随提交同行——gate 自产、已在落地物清单披露。
- (记录) ADR Status 引 D-001..D-005 为整轮批准范围（D-004 无本轮落地物），属引用非处置。

两轴评审后的修复已 amend 回 zvu（未发布本地提交，gitbutler 规则允许）；amend 后全套件重跑 1129/1129 绿。
