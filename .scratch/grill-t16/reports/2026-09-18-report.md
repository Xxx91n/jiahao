# grill-t16 round report — 2026-09-18

Round: fix + mechanism round (ledger D-001). Executor: repair/dev sub-agent.
Boundary commits: R1 documentation bundle `spr` + R2 implementation
bundle `wsn` (verifier fix) and the closeout commit, all on grill-t16-docs. Task book:
`.scratch/grill-t16/handoffs/next-round.md`.

## Facts (rendered from `.scratch/grill-t16/round-facts.json` — ADR-0077 D-C canon)

<!-- round-facts:start -->
- suites: 73
- passed: 1216
- skipped: 0
- pack_bytes: 333992
- instrument_entries: 27
- rewrite_map_citations: 1412
- registry_entries: 56
- anchors_count: 16
- battery_as_of_commit: f86494d
- report_commit: null
- not_run: [bench-gate, ci-wiring, mr-probes, probes]
<!-- round-facts:end -->

The report is narrative, never the home of numbers. `report_commit` stays
`null` by design — the report cannot cite its own commit. `battery_as_of_commit`
pins the tree the closing battery ran against.

## Findings settlement (t15 audit F-A..F-E)

- **F-A** (exit union across rows) — SETTLED. `scripts/check-ci-jobs.js` exit
  is keyed to `defer0004.satisfied` (the row currently consuming the verifier);
  defer0026 legs remain computed and printed as diagnostics and never drive
  the exit. A crash exits `2` on a stderr line — verifier-broken can no
  longer masquerade as unsatisfied (`evalSuggestions` already warns on exit>1).
- **F-B** (stale "multi-job" header) — SETTLED in the same edit: the header
  registers the consuming-row convention verbatim and the discharge re-point
  clause.
- **F-C** (defer-0004 citation chain broke) — SETTLED in R1: the rationale
  cites ADR-0058 D-C explicitly (the three-exit framework under which the
  narrowed re-defer was taken), same commit as the CONTEXT convention touch.
- **F-D** (mechanism outputs undeclared) — SETTLED by convention: ADR-0077
  D-B registers the `mechanism_outputs` closed enumeration (R2-internal, never
  a fourth surface), rewords `diff_semantics`, adds the optional
  `mechanism_output_diff` provenance marker, and annotates the t15 row
  additively (historical booleans untouched, `carve_out_used:1` stands).
- **F-E** (nits) — SETTLED: adr-0069 stale comment (rode the forced 76→77
  count bump in R1 — same physical line, disclosed), `surface-taxonomy.js`
  dead `m[1]||m[2]` dropped, `any_matrix` regex now matches the inline
  `matrix: {...}` form, t15 ledger citation count corrected 1346→1403 in both
  copies (anchors regenerated), CONTEXT carve-out gloss restored the
  trend-inventory channel.

## Decision mapping

- **D-002** — executed: B1 exit re-keyed + crash→2 + header rewrite +
  convention comment; B2 convention verbatim in the ledger T-1 section and
  CONTEXT terms (settle commit); B3 F-C citation in the same R1 commit.
- **D-003** — executed: `mechanism_outputs` enumeration `{file, generator,
  replay_verified}` = `src/instrument-state.json` (instrument.js, false —
  append-only timestamps) + `bench/research/out/g6-publish-replay.json`
  (check-g6-publish.js, true — byte-deterministic at a fixed tree). The same
  output class on the documentation surface (anchors.json, rewrite-map.json,
  the taxonomy R1.files snapshot, the README adr-index block) is named in
  ADR-0077, not enumerated: R3 files never enter files[] jurisdiction, so the
  exemption question does not arise — this is the honest reading of
  "R2-internal closed enumeration" + "every entry classifies R2" (the spec's
  member list named the whole output family; the enumeration governs exactly
  the set the disclosure obligation exists for). Checker support landed in R2:
  membership + R2 classification validated, bare marker hard-fails, never
  feeds burn-rate. The t16 row carries the marker's first real use
  (g6-publish-replay.json gate refresh during the R1 battery).
- **D-004** — executed: `scripts/build-round-facts.js` regenerates
  `.scratch/grill-t16/round-facts.json`; the facts section above renders
  deterministically inside the sentinel region; a render-pin wiring test
  asserts artifact-region equality and bans bare schema-key values in report
  prose. Addendum channel narrowed to genuinely subsequent events and
  self-reference disclosure (ASC 855 analogy — stale numbers are restated,
  not annotated); this report states the narrowing so the thinner addendum
  is not misread as a deletion. Adjudicated deviation: the artifact does NOT
  join the anchors chain (per-round regeneration would churn the digest
  chain) — recorded in ADR-0077 D-C.
- **D-005** — executed: light close (no strategic/promotion review, no
  G1/G3/G4 amendment); R1 all-R3 / R2 all-R2 — carve-out NOT invoked,
  burn-rate streak resets (`carve_out_used:0` on the t16 row); consent-sweep
  disposition lines written at T-1; zero owner asks; the three audit patches
  (tq/nl/xu) stay untracked forever; explicit-change-ID discipline restated:
  `but commit` was given the exact change IDs copied from `but status`
  output, never invented.

## Reproducible evidence

- `node scripts/check-ci-jobs.js` → exit 1; prints
  `defer0004=unmet(...) defer0026=SATISFIED(...) - not met (defer0004); the
  deferral remains valid` (consuming row keyed; defer0026 diagnostic).
- `node scripts/check-ci-jobs.js /nonexistent.yml` → exit 2; stderr
  `check-ci-jobs: verifier broken - ENOENT...` (crash never reads as
  unsatisfied).
- `node scripts/run-test-gate.js --expected-suites 73` →
  `[test] OK: 73 suites, 1216 tests, 0 skipped (ADR-0057 D-C)`, exit 0.
- `npm run gate:all` → `gate:all exit 0 (35 entries, 4 unverifiable,
  fail-fast off)`; UNVERIFIABLE = ci-wiring/bench-gate/probes/mr-probes
  (ci-mode absent on this host — registered, not skipped silently).
- `node scripts/check-governance-inventory.js` →
  `[governance-inventory] OK: 35 entries, live source_adr, unique commands,
  closed references`, exit 0 (one standing advisory: doc-round ADR streak).
- `node scripts/check-deferred.js` → `[deferred] OK - 56 entries (47 live,
  9 closed/actioned)`, exit 0.
- `node scripts/build-governance-anchors.js --check` →
  `OK - 16 artifacts, digests in sync` (decision-ledger-t16.md admitted).
- `node scripts/build-rewrite-map.js --check` → `map in sync`.
- `node scripts/check-ci-wiring.js` / adr-0058-wiring → suite green;
  the three exit directions + crash + defer0026-diagnostic + inline-matrix
  fixtures all pass.
- `npm pack` → `jiahao-0.0.1.tgz` under the 340000 budget; `pack-smoke`
  PASS (`330790 bytes, 112 files`) - the byte count lives in the facts
  artifact and the g6-publish replay record.
- `node scripts/build-round-facts.js --round grill-t16 --check --report
  .scratch/grill-t16/reports/2026-09-18-report.md` → facts + region in sync,
  exit 0.

## Disclosures

- `evalSuggestions` discards verifier stdout: a defer0026 leg regression
  surfaces only via wiring fixtures at runtime (wiring-layer-only, not
  orphaned — the summary job is separately covered by check-ci-wiring).
- The t16 ledger's governance copy (`docs/governance/decision-ledger-t16.md`)
  joined the anchors chain in R2 (the ARTIFACTS edit is machinery); it is
  resynced at round close so the digest pins the final ledger.
- The mechanism-output reading note under D-003 above is disclosed as an
  interpretation: the enumeration is exactly the R2-surface members; the
  docs-surface siblings are named in the ADR for completeness.
- The `mkz` settle commit's CONTEXT terms (Consuming-Row Exit, Facts Canon)
  predated this round's work and are pinned by wiring now.

## T-3 awareness (zero asks)

- Stack landing: `grill-t15-docs` carries the t15 + t16-settle commits
  unlanded; `grill-t16-docs` adds `spr` + the R2 commits on top. Landing is
  owner-domain — awareness only.
- defer-0060 stays a tracked row (quarterly, review_at 2026-12-15): the
  owner actions (secret regen + required-check deployment) are its
  unfreeze_if, not a decision ask.
- Sunset counter stays 1/6; next quarterly observation 2026-12-15.
