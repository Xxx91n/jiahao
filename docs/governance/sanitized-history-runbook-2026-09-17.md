# Sanitized-History Runbook — 2026-09-17 publish event

Registered under ADR-0074 D-A/D-B. This runbook records the facts of the
owner-ordered pre-publish history sanitization and the verification evidence
that bounds it. It never restates redacted literals or removed-file contents;
paths and object names are cited because they are the verification surface.

## Facts

- **Event**: owner-ordered history rewrite executed before first push;
  `origin/main` published at `051744a7a1b4027a42720814c819bf051e0831a8`
  (295 commits) on 2026-09-17.
- **Purged paths** (removed from every published tree):
  `.scratch/grill-t11/host-config-backup/` (one committed host-config backup
  file) and `jiahao-0.0.1.tgz` (a derived npm-pack tarball, 305,755 B,
  sha256 `c40db85d…` per the untrack commit message).
- **Rewrite region**: pre-purge side `05fa697..2c93a30` (retained local-only
  under `gb-local/grill-t12-docs`); published side `2e9cdc9..3454d13`. Both
  sides share base `1ca81f5`; everything below it is unchanged.
- **Empty-commit disposition**: `b73e558` (F-A1 tarball untrack, change-ID kxo)
  and `e54c267` (O-2 backup untrack, change-ID wko) survive as empty commits —
  their entire diff was removing purged paths.
- **Tip-local sanitization** (published-side-only commits): `a6729a9` redacted
  the host-topology literal in `.scratch/grill-t11/reports/2026-09-17-audit.md`
  and regenerated `.scratch/grill-t12/audit-evidence/round-diff.patch` without
  the purged-path hunks; `051744a` appended the closing-handoff addendum in
  `.scratch/grill-t12/handoffs/2026-09-17-reaudit-closed.md`.
- **Residual**: the redacted literal persists in the commit-message metadata
  of the tip-region commits. Bounded and disclosed (ADR-0074 D-A(c)); the
  value is not restated here — it lives in the published commit object.

## Verification evidence (re-runnable)

All commands run in the maintenance clone (it holds both history sides under
`gb-local/*` + `origin/*`); a fresh clone cannot resolve pre-purge names.

1. **Purged paths never appear in published history** (double-empty, registered forms):
   - `git log --name-status origin/main -- .scratch/grill-t11/host-config-backup/ jiahao-0.0.1.tgz` → no output (no published commit ever touched those paths).
   - `git log -S'<redacted-literal>' --oneline origin/main` → no output (substitute the literal from the owner's purge manifest; not restated here).
   - Supplementary: `git ls-tree -r origin/main --name-only | grep -E 'host-config-backup|jiahao-0\.0\.1\.tgz'` → no output.
   - Nuance: `-S` on a *path token* (e.g. `-S'jiahao-0.0.1.tgz'`) legitimately fires — docs/.gitignore diffs mention the name; the `-S` probe binds to the literal, not the path.
2. **Counterpart trees are identical across the boundary**:
   `git diff 2c93a30 3454d13` → empty output.
3. **Pre-purge tip → published tip equals the registered sanitization hunks
   and nothing else**: `git diff 2c93a30..051744a --stat` → exactly
   `.scratch/grill-t11/reports/2026-09-17-audit.md` (2 lines changed),
   `.scratch/grill-t12/audit-evidence/round-diff.patch` (58 changed),
   `.scratch/grill-t12/handoffs/2026-09-17-reaudit-closed.md` (+21).
4. **Before/after correspondence** (aligned by commit message; the full map is
   generated by `scripts/build-rewrite-map.js` per
   `docs/rewrite-map-generator-spec.md`):

   | pre-purge (local-only) | published | note |
   |---|---|---|
   | `05fa697` | `2e9cdc9` | first rewritten commit (purged paths entered here) |
   | `112686a` | `b73e558` | F-A1 untrack → empty commit (kxo) |
   | `3692a86` | `e54c267` | O-2 untrack → empty commit (wko) |
   | `2c93a30` | `3454d13` | pre-purge tip ↔ published counterpart, identical trees |
   | — | `a6729a9` | published-side-only (tip sanitization) |
   | — | `051744a` | published-side-only (addendum; publish tip) |

5. **Empty commits carry no diff**: `git show b73e558 --stat` and
   `git show e54c267 --stat` → no file rows.
6. **Literal-value absence at tip**: substitute the literal from the owner's
   purge manifest (not restated here) into
   `git grep -F '<literal>' origin/main` → no output expected. (A bare
   port-number fragment is NOT a valid probe — it matches unrelated
   pre-existing content.)

## Exposure statement

The purged objects were committed at `05fa697` on 2026-09-17 and never pushed
before the rewrite — zero external exposure window; they persist as
local-only objects under `gb-local/*` refs. Statements in earlier records that
cite `05fa697` describe those local objects.
